# 第一部分:HTML

## 一、HTML基础

### （一）HTML简介

+ HTML，全称`HyperText Markup Language`,超文本标记语言

---

### （二）HTML标签

#### ①标签

+ **标签**又称**元素**，是HTML的基本组成单位
+ 标签可以分为**双标签**和**单标签**，其中，绝大部分都是双标签，只有小部分为单标签
+ 标签名不区分大小写

<br>

#### ②属性

+ **属性**用于给标签提供附加信息
+ 可以写在起始标签或单标签中
+ 样例:

~~~html
    <marquee bgcolor="green"> 信息 </marquee>
    <input type="button">
    <input disabled> <!-- 有一小部分特殊属性没有属性名，只有属性值 -->
~~~

---

### （三）HTML注释

+ 格式:`<!-- 注释 -->`
+ 作用:对代码进行解释和说明
+ 例:

~~~html
    <!-- 该注释在源代码中可见，但不会作用于网页 -->
    <marquee bgcolor="green"> 信息 </marquee>
~~~

---

### （四）HTML基本架构

#### ①HTML文档声明

1. 作用:告诉浏览器当前网页的版本，即该`HTML`代码是按照什么标准写的
2. 格式(仅`HTML5`):`<!DOCTYPE html>`
3. 该文档并不是`HTML`标签，需要写在`HTML`源代码的最上方，且在`HTML`标签外

---

#### ②HTML字符编码

+ 计算机在编码与解码时，会按照特定的字符集进行操作，如果操作不当可能会造成**数据丢失**或**乱码**
  1. 数据丢失:计算机在使用指定字符集存储对应文本进行编码时，由于所写的文本与字符集所支持的字符不一致，导致数据无法编码成功从而出现数据丢失现象，由于字符集缺少文本中的字符的对应编码，因此出现数据丢失的大部分情况是出现`?`字符。如果出现数据丢失，是无法再找回数据的，因为编码已经出现错误了。
  2. 乱码:计算机在进行文件读取时，使用了不同于文件存储时的字符集对文件进行解码操作，从而使文件看起来非常乱。这种操作可以通过更改对应字符集解决。
+ `GB2312`字符集支持大部分汉字；`GBK`字符集支持的汉字范围更广，且支持繁体中文；`UTF-8`编码基本上支持世界上绝大多数国家所使用的字符
+ 浏览器默认使用`UTF-8`字符集对`HTML`文件进行解码
+ 可以通过`meta`标签的`charset`属性设置设置字符编码的格式:`<meta charset="对应字符集">`

---

#### ③设置语言

+ 一般来说，在网页语言单一时，浏览器可以自动检测到当前语言并弹出提示询问是否需要转换语言
+ 但如果网页中的语言种类较多时，浏览器会反应不过来，从而难以翻译，此时就需要在HTML源代码中手动告诉浏览器应该使用哪种语言进行翻译
+ 可以通过`html`标签中的`lang`属性设置语言:`<html lang="语言">`
<a id="element_lang"></a>
+ 以下为一些常用语言的对应lang属性值:

|语言|属性值|
|---|---|
|英语|`en`|
|中文|`zh`|
|简体中文|`zh-CN`(推荐)/`zh-Hans`(不推荐)|
|繁体中文|`zh-TW`(推荐)/`zh-Hant`(不推荐)|
|美式英语|`en-US`|
|英式英语|`en-UK`|

+ 其它详情参考[语言代码](https://www.w3school.com.cn/tags/html_ref_language_codes.asp)和[国家/地区代码](https://www.w3school.com.cn/tags/html_ref_country_codes.asp)
+ 使用该属性可以让浏览器显示对应的翻译提示，同时有利于搜索引擎的优化

---

#### ④meta元信息

+ meta标签可以让浏览器知道网页的基本信息

|功能|代码|备注|
|:---:|:---:|:---:|
|配置字符编码|`<meta charset="utf-8">`|
|针对 IE 浏览器的兼容性配置|`<meta http-equiv="X-UA-Compatible" content="IE=edge">`|
|针对移动端的配置|`<meta name="viewport" content="width=device-width, initial-scale=1.0">`|
|配置网页关键字|`<meta name="keywords" content="几个个以英文逗号隔开的单词/词语">`|
|配置网页描述信息|`<meta name="description" content="一段话，与网站内容相关">`|
|针对搜索引擎爬虫配置|`<meta name="robots" content="此处可选值见后表">`|
|配置网页作者|`<meta name="author" content="tony">`|
|配置网页生成工具|`<meta name="generator" content="Visual Studio Code">`|
|配置定义网页版权信息|`<meta name="copyright" content="2023-2027©版权所有">`|
|配置网页自动刷新|`<meta http-equiv="refresh" content="10;url=http://www.baidu.com">`|
|其它详见[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/meta)|

|content值|描述|
|:---:|:---:|
|`index`|允许搜索爬虫索引此页面|
|`noindex`|要求搜索爬虫不索引此页面|
|`follow`|允许搜索爬虫跟随此页面上的链接|
|`nofollow`|要求搜索爬虫不跟随此页面上的链接|
|`all`|与`index`,`follow`等价|
|`none`|与`noindex`,`nofollow`等价|
|`noarchive`|要求搜索引擎不缓存页面内容|
|`nocache`|`noarchive`的替代名称|

---

#### ⑤标准结构

~~~html
    <!DOCTYPE html>
    <html lang="zh-CN"> <!-- 提示浏览器怎么翻译 -->
    <head>
        <meta charset="UTF-8">  <!-- 告知浏览器如何解码 -->
        <title>Document</title>  <!-- 设置标题 -->
    </head>
    <body>
        
    </body>
    </html>
~~~

---

#### ⑥开发者文档

+ 可以通过以下方式了解更多`HTML`相关知识
  1. [W3C官网:妈的，根本不知道这逼玩意的文档在哪](https://www.w3.org/)
  2. [W3School](https://www.w3school.com.cn/index.html)
  3. [MDN](https://developer.mozilla.org/zh-CN/)

---

## 二、HTML4

### （一）行内元素与块级元素

#### ①特性与使用规则

+ 行内元素:不独占一行
+ 块级元素:独占一行
+ 使用规则:
  1. 使用**行内元素**时，内部可以嵌套行内元素，但**不能嵌套块元素**
  2. 使用**块级元素**时，内部可以嵌套行内元素，也可以嵌套块元素(几乎全都可以嵌套)

<br>

#### ②元素表

+ 块元素

|标签名|描述|备注|
|---|---|---|
|`h1-h6`|标题|标题标签不能互相嵌套|
|`p`|段落|**p标签不能嵌套块元素**|
|`div`|容器|无|

+ 行内元素

|标签名|类型|描述|备注|
|---|---|---|---|
|`input`|输入框|无|

---

### （二）排版与文本标签

#### ①排版标签

|标签名|描述|备注|所属类型|
|---|---|---|---|
|`h1-h6`|标题|无|块级元素|
|`p`|段落|无|^|

<br>

#### ②文本标签

> 文本标签用于包裹词汇、短语等

+ 文本标签通常都是行内元素，且通常写在排版标签内部

|是否常用|标签名|描述|详解|备注|所属类型|
|---|---|---|---|---|---|
|常用|`strong`|修饰重要文本|[strong标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/strong)|---|行内元素|
|^|`b`|用于吸引读者的注意到该元素的内容上,通常用于|[b标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/b)|以前这个元素过去被认为是粗体（Boldface）元素，并且大多数浏览器仍然将文字显示为粗体。尽管如此，不应将元素用于显示粗体文字|行内元素|
|^|`i`|修饰区分普通文本，通常为斜体|[i标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/i)|现在多用于呈现字体图标|行内元素|
|^|`ins`|下划线|[ins标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/ins)|无|行内元素|
|^|`sup/sub`|设置上标/下标|[sup标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/sup),[sub标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/sub)|无|行内元素|
|^|`del`|删除线|[del标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/del)|无|行内元素|
|^|`code`|代码块|[code标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/code)|无|行内元素|
|^|`em`|标记需要用户着重阅读的内容|[em标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/em)|无|行内元素|
|不常用|`abbr`|修饰缩写|[abbr标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/abbr)|需要`title`属性配合使用,`title`属性可以设置在鼠标移至该文本时显示指定文本|行内元素|
|^|`samp`|通常用于标识计算机程序输出|[samp标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/samp)|无|行内元素|
|^|`kbd`|修饰键盘文本|[kbd标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/kbd)|无|行内元素|
|^|`var`|标记变量|[var标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/var)|无|行内元素|
|^|`bdo`|更改文本方向|[bdo标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/bdo)|通常与dir属性一起使用,dir属性值为ltr(left to right)和rtl(right to left)|行内元素|
|^|`small`|修饰附属细则，如版权、法律声明等|[small标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/small)|无|行内元素|
|^|`u`|修饰与正常内容有反差文本，如:错误的单词，不合适的描述|[u标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/u)|无|行内元素|
|^|`q`|短引用|[q标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/q)|无|行内元素|
|^|`blockquote`|长引用|[blockquote标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/blockquote)|无|块元素|
|^|`address`|修饰地址信息|[address标签详解](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/address)|无|块元素|

#### ③杂项

|标签名|描述|备注|所属类型|
|---|---|---|---|
|`br`|换行|无|不知道|
|`hr`|分割线|无|块元素|
|`pre`|保留原文|浏览器会识别pre标签内文本的全部空格与换行并呈现|块元素|

---

### （三）图片标签

#### ①基本使用

|标签名|含义|所含属性|属性含义|备注|标签类型|
|---|---|---|---|---|---|
|`img`|展示图片|`src`|读取图片路径|无|行内块元素|
|^|^|`alt`|1.向搜索引擎提供图片具体细节，利于搜索引擎优化和提高网页曝光性<br>2.在图片无法加载时向用户展示alt属性里的内容|无|^|
|^|^|`width`|调整图片宽度|若仅调整宽度，图像的高度也会等比例缩放|^|
|^|^|`height`|调整图片高度|若仅调整高度，图像的宽度也会等比例缩放|^|

#### ②图片路径

+ 补充
> 1. 可以通过`head`标签内的`base`标签的`src`属性，来定义所有路径的**公共前缀**，该前缀会在浏览器请求资源时**自动插入到其它标签定义的相对路径之前，但对绝对路径无效**

##### Ⅰ相对路径

|符号|含义|备注|
|---|---|---|
|`./`|同级|可省略|
|`/`|下一级|无|
|`../`|上一级|无|

+ 相对路径依赖当前位置，若当前位置发生更改，则相对路径也要对应更改

<br>

##### Ⅱ绝对路径

+ 绝对路径分为两种:
  1. 本地绝对路径:即在本地电脑中的路径，从盘符开始，格式:`C:/xx/xx/xx/`
  2. 网络绝对路径:根据网址读取文件
+ 使用本地路径，一旦更换设备，路径处理较麻烦
+ 使用网络绝对路径读取图片时，若服务器开启了防盗链，那么会造成图片引用失败

<br>

##### Ⅲ常用图片格式

|格式|描述|后缀|特点|使用场景|备注|
|---|---|---|---|---|---|
|`jpg`|有损压缩格式|`.jpg/.jpeg`|1.支持颜色丰富<br>2.占用空间较小<br>3.不支持透明背景<br>4.不支持动态图|对图片细节要求不高的场景，如:产品宣传图|无|
|`png`|无损压缩格式|`.png`|1.支持颜色丰富<br>2.占用空间略大<br>3.支持透明背景<br>4.不支持动态图|1.想让图片有透明背景<br>2.想呈现更高质量的图片,如:公司logo|无|
|`bmp`|无损不压缩的格式，在最大程度上保留图片更多的细节|`.bmp`|1.支持颜色丰富<br>2.保留细节更多<br>3.占用空间极大<br>4.不支持透明背景<br>5.不支持动态图|对图片细节要求极高的场景:一些大型游戏中的图片|无|
|`gif`|无|`.gif`|1.支持颜色较少<br>2.支持简单透明的背景图<br>3.支持动态图|网页中的动态图片|无|
|`webp`|谷歌推出的一种图片格式，专门用来在网页中呈现图片|`.webp`|具备上述几种格式的优点，但兼容性不太好|网页中的各种图片|无|
|`base64`|本质是一串特殊的文本，要通过浏览器打开，看图软件一般无法打开|本质为文本，故无后缀|一串特殊文本|一些较小的图片或需要和网页一起加载的图片|1.可以通过一些工具或网站将图片进行`base64`编码，形成一串文本<br>2.使用时，将此文本直接放入`img`标签的`src`属性中即可|

---

### （四）超链接

+ 超链接的主要作用是从当前页面进行跳转

|标签名|含义|常用属性|属性含义|属性值|标签类型|备注|
|---|---|---|---|---|---|---|
|`a`|超链接|`href`|传入要跳转的路径|跳转路径|行内元素|1. 虽然a是行内元素，但是它可以嵌套除它以外的其它任何标签<br>2. 代码中的空格和回车会被浏览器解析成一个空格，因此会造成两个标签之间有间隙|
|^|^|`target`|跳转时如何打开页面|1. `_blank` 在新窗口中打开<br>2.`_self` 在被点击时的本页面打开链接（默认）<br>3.`iframe`标签的`name`值 在网页的`iframe`框架内打开|^|^|

#### ①跳转到页面

~~~html
  <!-- 设置一个超链接，使用新页面打开方式，跳转到百度 -->
  <a href="https://www.baidu.com/" target="_blank">百度</a>
~~~

#### ②跳转到文件

~~~html
  <!-- 使用a标签强制下载文件，download属性用于浏览器强制下载，其属性值为下载好后的默认文件名称 -->
  <a href="../test.html" download="HTML文件">下载文件</a>

  <!-- 使用a标签打开文件 -->
  <a href="../../../笔记类/各学科笔记/web前端/新web前端笔记.md">前端笔记</a>

  <!-- 使用a标签打开不能打开的文件时，浏览器因不能打开此文件会默认下载该文件 -->
  <a href="../作业/实验5/实验5空白.docx">docx文件</a>
~~~
+ <a href="../笔记代码/源码/HTML+CSS/超链接_跳转文件.html">代码示例</a>

<br>

#### ③跳转到锚点

1. 设置锚点:
   + 可以使用`<a name="xxx"></a>`这种a标签与name属性配合的方式设置锚点
   + 可以使用`<其他标签 id="xxx"></其他标签>`这种其他标签与id属性配合的方式设置锚点
   + `name`与`id`属性都区分大小写，id最好不要为数字开头
2. 跳转锚点
   + 在`a`标签的`href`属性中使用`#xxx`的格式即可完成超链接跳转设置
   + 只使用#号跳转到页面最上方
   + `href`为空会刷新页面
   + 可以对`href`属性设置`网页路径#对应标签id/name`来跳转到指定页面的指定锚点处
   + `href`属性还可以直接支持JS脚本

+ <a href="../笔记代码/源码/HTML+CSS/超链接_跳转锚点.html">代码示例</a>

<br>

#### ④跳转到指定应用

+ 示例:

~~~html
    <!-- 唤起设备拨号 -->
    <a href="tel:10010">给客服打电话</a>
    <!-- 唤起设备发送邮件 -->
    <a href="mailto:aaa@qq.com">给我发邮件</a>
    <!-- 唤起设备发送短信 -->
    <a href="sms:10086">给客服发短信</a>
~~~

+ <a href="../笔记代码/源码/HTML+CSS/超链接_跳转指定应用.html">网页示例</a>

---

### （五）列表

|列表类型|标签|标签含义|备注|所属类型|
|---|---|---|---|---|
|有序列表|`ol`|定义有序列表|无|待施工|
|无序列表|`ul`|定义无序列表|无|^|
|有序/无序列表|`li`|包裹列表内容|li最好不要单独使用|^|
|自定义列表|`dl`|定义自定义列表|无|^|
|^|`dt`|描述术语名称|无|^|
|^|`dd`|术语详细描述|无|^|

#### ①有序列表

+ 有序列表使用标签`ol`来定义，且其内部嵌套的项使用标签`li`来包裹内容
+ 示例:

~~~html
    <h2>怎么睡觉</h2>
    <!-- 有序列表定义 -->
    <ol>
        <li>躺在床上</li>  <!-- 使用li标签包裹内容，li标签最好在列表中使用，不要单独使用 -->
        <li>闭上眼睛</li>
        <li>开始睡觉</li>
    </ol>
~~~

<br>

#### ②无序列表

+ 有序列表使用标签`ul`来定义，且其内部嵌套的项使用标签`li`来包裹内容
+ 示例:

~~~html
    <h2>中国的大城市有</h2>
    <!-- 无序列表定义 -->
    <ul>
        <li>北京</li>
        <li>上海</li>
        <li>广州</li>
        <li>深圳</li>
    </ul>
~~~

<br>

#### ③自定义列表

+ 自定义列表使用`dl`来定义，使用标签`dt`来描述术语名称，使用标签`dd`来进行术语描述
+ 样例:

~~~html
    <!-- 自定义列表定义 -->
    <h2>如何快乐学习</h2>
    <dl>
        <dt>学习一点也不快乐</dt>  <!-- 描述术语名称 -->
            <dd>学习会占用大量时间</dd>  <!-- 术语详细描述 -->
            <dd>长时间学习容易使人厌烦</dd>
            <dd>想摆烂</dd>
        <dt>想玩</dt>
            <dd>娱乐使人快乐</dd>
    </dl>
~~~

+ <a href="../笔记代码/源码/HTML+CSS/列表.html">网页示例</a>

---

### （六）表格

#### ①表格的基本结构

|标签|含义|备注|
|---|---|---|
|`table`|设置表格|无|
|`caption`|设置表格标题|无|
|`thead`|设置表格头部|无|
|`tbody`|设置表格主体|无|
|`tfoot`|设置表格脚注|无|
|`tr`|添加行|无|
|`th`|添加单元格(列)|在thead标签内使用|
|`td`|^|在`tbody`和`tfoot`标签内使用|

<br>

#### ②表格的常用属性

|标签|属性|含义|属性值|备注|网页示例|
|---|---|---|---|---|---|
|`table`|`width`|设置表格宽度|以像素为单位的数字|无|[表格1](../笔记代码/源码/HTML+CSS/表格_常用属性.html)|
|^|`height`|设置表格**最小**高度|以像素为单位的数字|无|^|
|^|`border`|设置表格边框宽度|以像素为单位的数字|`border`的控制范围只为表格的外边框，不能控制表格内单元格的边框宽度|^|
|^|`cellspacing`|设置单元格之间的间距|以像素为单位的数字|无|^|
|`caption`|占位|占位|占位|使用`CSS`调整|^|
|`thead`|`height`|设置表格头部高度|以像素为单位的数字|无|^|
|^|`align`|设置单元格**水平**对齐方式|1.`left` 左对齐<br>2.`center` 居中对齐<br>3.`right` 右对齐|无|^|
|^|`valign`|设置单元格**垂直**对齐方式|1.`top` 顶部对齐<br>2.`middle` 中间对齐<br>3.`bottom` 底部对齐|无|^|
|`tbody`|常用属性与`thead`相同|占位|占位|无|^|
|`tfoot`|常用属性与`thead`相同|占位|占位|无|^|
|`tr`|常用属性与`thead`相同|占位|占位|无|^|
|`td`|`width`|设置单元格宽度|以像素为单位的数字|1.同列全部单元格全部受影响<br>2.若`td`设置的`width`属性比`table`设置的`width`属性小，浏览器会依据实际情况(除去设置`width`的标签，其它标签能否在宽度最小的情况下在`table`的`width`剩余值内存在)决定按照哪个设置来呈现表格<br>3.若`td`设置的`width`属性比`table`设置的`width`属性大，会将该`td`扩充到最大可接受范围，但总表格`width`依旧为`table`设置的`width`值|^|
|^|`height`|设置单元格高度|以像素为单位的数字|1.同行全部单元格全部受影响<br>2.若`td`设置的`height`属性比`thead`/`tbody`设置的`height`属性小，浏览器会依据`thead`/`tbody`的`height`属性为标准呈现表格<br>3.若`td`设置的`width`属性比`thead`/`tbody`设置的`height`属性大，浏览器会依据td的height属性为标准呈现表格|^|
|^|`align`|设置单元格**水平对齐方式**|1.`left` 左对齐<br>2.`center` 居中对齐<br>3.`right` 右对齐|无|^|
|^|`valign`|设置单元格**垂直对齐方式**|1.`top` 顶部对齐<br>2.`middle` 中间对齐<br>3.`bottom` 底部对齐|无|^|
|^|`rowspan`|指定要跨的行数|要跨的行数|无|[表格2](../笔记代码/源码/HTML+CSS/表格_跨行与跨列.html)|
|^|`colspan`|指定要跨的列数|要跨的列数|无|^|
|`th`|常用属性与`th`相同|占位|占位|无|[表格1](../笔记代码/源码/HTML+CSS/表格_常用属性.html)|

---

### （七）表单

#### ①表单基础标签与属性

|标签名|含义|属性|属性含义|属性值|属性备注|标签备注|网页示例|
|---|---|---|---|---|---|---|---|
|`form`|表单|`action`|用于指定接收并处理表单数据的服务器程序的url地址|url地址|action中的url既可以是绝对路径也可以是相对路径,还可以为接收数据的E-mail邮箱地址。|无|[表单基本结构](../笔记代码/源码/HTML+CSS/表单_基本结构.html)|
|^|^|`target`|控制表单提交后，如何打开界面|1. `_blank` 在新窗口中打开<br>2.`_self` 在被点击时的本页面打开链接(默认)|无|无|^|
|^|^|`method`|控制表单的提交方式|1.`get`(默认)<br>2.`post`|1.使用`get`属性值在提交后会显示在浏览器的地址栏中，保密性差，且有数据量的限制<br>2.使用`post`属性值提交的数据保密性好，并且无数据量的限制，可以大量提交数据|无|^|
|^|^|`name`|指定表单的名称，以区分同一个页面中的多个表单|表单名称|无|无|^|
|^|^|`enctype`|指定表单在将数据发送到服务器之前如何对表单数据进行编码|编码形式|无|无|^|
|`input`|输入框|`type`|定义输入框类型|text&nbsp;定义单行输入字段，可输入字母、数字等内容，默认20个字符|无|无|[表单_输入框](../笔记代码/源码/HTML+CSS/表单_输入框.html)|
|^|^|^|^|`password`&nbsp;定义密码字段，用于输入密码，输入内容以*的形式出现（掩码）|无|^|^|
|^|^|^|^|`radio`&nbsp;单选框|无|^|^|
|^|^|^|^|`checkbox`&nbsp;复选框|无|^|^|
|^|^|^|^|`button`&nbsp;定义普通按钮，用于通过`JavaScript`启动脚本|无|^|^|
|^|^|^|^|`submit`&nbsp;定义“提交”按钮，将表单发送到服务器|无|^|^|
|^|^|^|^|`reset`&nbsp;定义“重置”按钮，点击重置按钮可清楚表单中的所有数据|无|^|^|
|^|^|^|^|`image`&nbsp;定义图像形式的提交按钮，用户可以通过不同的图像达到美观的效果|无|^|占位|
|^|^|^|^|`hidden`&nbsp;定义隐藏的输入字段|无|^|[表单_输入框](../笔记代码/源码/HTML+CSS/表单_输入框.html)|
|^|^|^|^|`file`&nbsp;定义输入字段和“浏览”按钮，用于上传文件|无|^|占位|
|^|^|^|^|`email`&nbsp;用于输入单个电子邮件地址或电子邮件地址列表。多个电子邮件地址可以使用逗号分隔的列表形式输入|无|^|占位|
|^|^|^|^|`color`&nbsp;用于使用色盘选择颜色|无|^|占位|
|^|^|^|^|`date`&nbsp;用于输入日历日期|无|^|占位|
|^|^|^|^|`datetime`&nbsp;用于输入时区为：格林尼治/国际标准时间的日期和时间|无|^|占位|
|^|^|^|^|`datetime-local`&nbsp;用于输入本地日期和时间|无|^|占位|
|^|^|^|^|`month`&nbsp;输入年份和月份|无|^|占位|
|^|^|^|^|`time`&nbsp;用于输入由时、分、秒以及小数秒组成的时间|无|^|占位|
|^|^|^|^|`week`&nbsp;输入由年份与周期构成的日期|无|^|占位|
|^|^|^|^|`number`&nbsp;输入数字|无|^|占位|
|^|^|^|^|`range`&nbsp;该控件类型以拖动条的形式呈现，使用户可以从一个数字范围内选择取值|无|^|占位|
|^|^|^|^|`search`&nbsp;用于输入关键词|无|^|占位|
|^|^|^|^|`tel`&nbsp;输入电话号码|无|^|占位|
|^|^|^|^|`url`&nbsp;输入网站URL|无|^|占位|
|^|^|`name`|定义数据名称|名称|1.在提交时，如果未写`name`属性，浏览器会找不到该数据的名称而无法提交<br>2.如果在`button`按钮或类型为按钮的input标签内写name属性的话，浏览器也会认为这是要提交的数据从而在提交时与其他数据一起提交<br>3.在单选框与复选框中，需要使`name`的值保持一致，这会让他们从属同一个单选/复选组|^|[表单_输入框](../笔记代码/源码/HTML+CSS/表单_输入框.html)|
|^|^|`value`|定义input输入框的默认值|输入的默认值|1.在单选框与复选框中，必须要写`value`属性，因为浏览器需要读取其`value`属性作为上传数据的值<br>2.在按钮中，`value`属性中的值可以指定按钮文字|^|^|
|^|^|`maxlength`|限制输入框最大输入字符数|数值|无|^|^|
|^|^|`placeholder`|为`input`类型的输入框提供用户提示|文本|无|^|占位|
|^|^|`required`|规定输入框填写的内容不能为空|无|无|^|占位|
|^|^|`readonly`|控件内容为只读（不能编辑修改）|无|无|^|占位|
|^|^|`disabled`|禁用该控件（显示为灰色）|无|使用`disbled`属性可以禁用`input`、`textarea`、`button`、`select`、`option`等标签|^|占位|
|^|^|`multiple`|指定输入框是否可以选择多个值|无|无|^|占位|
|^|^|`checked`|定义选择控件默认被选中的项|无|无|^|占位|
|^|^|`size`|input控件在页面中的显示宽度|数值|无|^|占位|
|`button`|按钮|`type`|定义按钮类型|1.`button` 普通按钮，一般需要通过`JavaScript`配合使用<br>2.`submit` 提交按钮<br>3.`reset` 重置按钮|不写此属性默认类型为`submit`|`button`标签不应写`name`属性，如果写了的话，在表单提交时系统会认为该按钮也是一个数据项并将其提交|[表单-输入框](../笔记代码/源码/HTML+CSS/表单_输入框.html)|
|`textarea`|多行文本框|`cols`|控制文本框的宽|数值|无|无|^|
|^|^|`rows`|控制文本框的高|数值|无|无|^|
|^|^|`name`|定义数据名称|名称|无|无|^|
|`select`|复选框|`name`|定义数据名称|名称|无|无|^|
|`option`|复选框选项|`value`|要传入的数据值|数据|无|无|^|
|`label`|使对应表单标签与`label`标签所包含文本相关联|`for`|要关联的标签`id`|`id`|无|不建议将按钮与文本关联|^|
|`fieldset`|给表单控件分组|占位|占位|占位|占位|无|[表单_分组](../笔记代码/源码/HTML+CSS/表单_分组.html)|
|`legend`|设置分组标题|占位|占位|占位|占位|无|^|

---

### （八）框架标签

+ 框架标签`iframe`可以在网页上嵌入内容(广告或其它内容)
+ `iframe`标签为**行内元素**

|属性|属性含义|属性值|备注|
|---|---|---|---|
|`width`|调整框架的宽|数值|无|
|`height`|调整框架的高|数值|无|
|`frameborder`|设置框架的边框是否显示|仅有`0`和`1`两个值，`0`为无边框，`1`为有边框|设置其它值默认为有边框|
|`name`|框架名称|文本|可以与超链接或表单的`target`属性配合，展示不同内容|

+ <a href="../笔记代码/源码/HTML+CSS/框架标签.html">网页示例</a>

---

<a id="entities"></a>

### （九）字符实体

+ `HTML`中可以用一种特殊的形式的内容，来表示某个符号，这种特殊形式的内容，就是HTML实
体。
+ 常见的字符实体如下:

|字符|描述|实体格式|编号|
|:---:|:---:|:---:|:---:|
|">"|大于号|`&gt;`|`&#62;`|
|"<"|小于号|`&lt;`|`&#60;`|
|"&nbsp;"|空格|`&nbsp;`|`&#160;`|
|"&"|与|`&amp;`|`&#38;`|
|"|双引号|`&quot;`|`&#34;`|
|'|单引号|`&acute;`|`&#180;`|
|其它参考<a href="https://www.w3school.com.cn/charsets/ref_html_8859.asp">w3school的实体表</a>

---

### （十）全局属性

+ 以下为常用的全局属性:

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`id`|给标签指定唯一标识，不可重复,一般配合CSS或JavaScript使用|文本|不能在`<head>`、`<html>`、`<meta>`、`<script>`、`<style>`、`<title>`标签中使用|
|`class`|给标签指定类名，随后通过CSS即可设定样式|类名|无|
|`style`|给标签设置CSS样式|CSS代码|无|
|`dir`|内容的方向|1.`ltr`&nbsp;2.`rtl`|1.对于不同的标签，实现的效果不同<br>2.不能在`<head>`、`<html>`、`<meta>`、`<script>`、`<style>`、`<title>`标签中使用|
|`title`|鼠标悬浮在对应标签上方时显示文字提示，一般用于超链接与图片|文本|无|
|`lang`|给标签指定语言|[详情查看前文](#element_lang)|不能在`<head>`、`<meta>`、`<script>`、`<style>`、`<title>`标签中使用|
|更多内容查阅[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/HTML),在**参考**的**全局属性**中有详细的内容|

---

## 三、HTML5

### （一）H5简介

#### ①官网

+ [官网1](https://html.spec.whatwg.org/multipage/index.html)
+ [官网2](https://whatwg-cn.github.io/HTML+CSS/multipage/)

---

#### ②H5优势

> 1. 针对`JavaScript`，新增了很多可操作的接口
> 2. 新增了一些语义化标签、全局属性
> 3. 新增了多媒体标签，可以很好的替代`flash`
> 4. 更加侧重语义化，对于`SEO`(搜索引擎优化)更友好
> 5. 可移植性好，可以大量应用在移动设备上

---

#### ③H5兼容性

> + `chrome`、`Safari`、`Opera`、`Firefox`等主流浏览器都支持`H5`
> + `IE`浏览器只有在9及以上版本才支持`HTML5`,且`IE9`仅支持部分`HTML5`新特性

---

### （二）新增标签

#### ①新增布局标签

|标签|含义|备注|
|:---:|:---:|:---:|
|`header`|整个页面，或部分区域的头部|无|
|`footer`|整个页面，或部分区域的底部|^|
|`nav`|导航|^|
|`article`|文章、帖子、杂志、新闻、博客、评论等|1.`article`标签里可以有多个`section`标签<br>2.`article`标签更强调独立性|
|`section`|页面中的某段文字，或文章中的某段文字|1.`section`强调的是分段或分块，如果想将一段内容分成几段，可使用`section`标签|
|`aside`|侧边栏|无|
|`main`|文档的主要内容|1.在`WHATWG`的官网中，此标签没有含义<br>2.`IE`浏览器不支持|
|`hgroup`|包裹连续的标题，如文章主标题、副标题的组合|`W3C`将其删除|
> + 以上元素皆为块元素

---

#### ②新增状态标签

|标签|含义|备注|样例|类型|
|:---:|:---:|:---:|:---:|:---:|
|`meter`|定义已知范围内的标量测量。也被称为`gauge`（尺度）|常用来表示电量、磁盘用量等|[样例](../笔记代码/源码/HTML+CSS/HTML5_状态标签1.html)|行内块元素|
|`progress`|显示某个任务完成的进度的指示器|一般用来表示任务完成进度|[样例](../笔记代码/源码/HTML+CSS/HTML5_状态标签2.html)|行内块元素|

|属性|含义|备注|
|:---:|:---:|:---:|
|`value`|当前值|无|
|`max`|上限值|^|
|`min`|下限值|^|
|`low`|规定低值|在没有`optimum`属性时，浏览器会认为`low`到`high`之间的数值为正常值，显示为绿色，其他不属于此区间的值为非正常值，显示为黄色|
|`high`|规定高值|^|
|`optimum`|规定最优值|使用了`optimum`属性后，浏览器会参考`optimum`属性在哪个区间，从而确定该区间为正常区间，显示为绿色，与此区间相邻的区间显示为黄色，剩余区间显示为红色|

---

#### ③新增列表标签

|标签|含义|类型|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`datalist`|用于搜索框的关键字提示|`display: none;`|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_列表标签.html)
|`detail`|用于展示问题和答案，或对专有名词进行解释|块元素|无|^|
|`summary`|写在`details`的里面，用于指定问题或专有名词|`list-item`|无|^|

---

#### ④新增文本标签

|标签|含义|类型|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`ruby`|包裹需要注音的文字|行内元素|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_文本标签.html)|
|`rt`|写注音|块元素|`rt`标签写在`ruby`的里面|^|
|`mark`|标记|行内元素|无|^|

---

#### ⑤新增表单控件属性

|属性|功能|备注|样例|
|:---:|:---:|:---:|:---:|
|`placeholder`|提示文字|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_新增表单控件属性.html)|
|`required`|表示该输入项必填,适用于**除按钮外**其他表单控件|若该属性作用于单选框，则表示该单选框必须选择其中一个属性，若该属性作用于多选框，则表示该多选框的这个选项必须被选中|^|
|`autofocus`|自动获取焦点，适用于所有表单控件|若写了多个`autofocus`,则浏览器会聚焦到第一个拥有`autofocus`属性的标签身上|^|
|`autocomplete`|自动完成，可以设置为`on`或`off`，适用于**文字输入类**的表单控件|密码输入框、多行文本框不可用|^|
|`pattern`|使用正则表达式限制**文字输入类**的表单控件的输入形式|多行输入不可用，且空的输入框不会验证，往往与`required`配合|^|

---

#### ⑥form和input标签新增属性或属性值

|属性/值|含义|所属标签|所属标签属性|备注|样例|
|:---:|:---:|:---:|:---:|:---:|:---:|
|`novalidate`|禁止表单对属性值进行合法性验证|`form`|自己就是属性|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_input和form新增的属性和属性值.html)|
|`email`|邮箱类型的输入框|`input`|`type`|表单提交时会验证格式，支持空输入提交(若不写`required`属性)|^|
|`url`|网址类型的输入框|^|^|表单提交时会验证格式，支持空输入提交(若不写`required`属性)|^|
|`number`|数字类型的输入框|^|^|1.表单提交时会验证格式，支持空输入提交(若不写`required`属性)<br>2.支持`max`、`min`、`step`等属性，若写了`step`属性，则`属性值-min`必须能被`step`的值整除|^|
|`search`|搜索类型的输入框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`tel`|电话类型的输入框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`range`|范围选择框|^|^|1.表单提交时不会验证格式，支持空输入提交(若不写`required`属性)<br>2.支持`max`、`min`、`step`等属性，若写了`step`属性，则`属性值-min`必须能被`step`的值整除|^|
|`color`|颜色选择框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`date`|日期选择框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`month`|月份选择框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`week`|周选择框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`time`|时间选择框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|
|`datetime-local`|日期+时间框|^|^|表单提交时不会验证格式，支持空输入提交(若不写`required`属性)|^|

---

#### ⑦视频标签

+ 使用`video`标签来向网页插入视频

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`src`|读取视频文件路径|文件路径|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_视频标签.html)|
|`height`|调整视频高度，宽度会等比例缩放|像素值|无|^|
|`width`|调整视频宽度，高度会等比例缩放|像素值|无|^|
|`controls`|显示视频控件|无属性值|无|^|
|`muted`|设置视频静音|无属性值|无|^|
|`autoplay`|设置视频自动播放|无属性值|**该属性发挥作用需要视频静音**，但若该网页的**媒体参与度比较高**的话(可以理解为用户在同一网页播放该视频的频率过高)，可能不设置静音也会播放视频|^|
|`loop`|视频循环播放|无属性值|无|^|
|`poster`|设置视频封面|文件路径|封面比例最好与视频比例一致，不然可能播放后视频大小会缩放影响观感|^|
|`preload`|设置视频预加载|1.`none`:不对视频做任何预加载处理<br>2.`metadata`:仅获取视频的基本信息(如播放时长等)，但有时也会预加载一部分内容<br>3.`auto`:预加载整个视频文件，即使用户不希望这样做|无|^|

---

#### ⑧音频标签

+ 使用`audio`标签来向网页插入音频

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`src`|读取音频文件路径|文件路径|无|[样例](../笔记代码/源码/HTML+CSS/HTML5_音频标签.html)|
|`controls`|显示音频控件|无属性值|无|^|
|`muted`|设置音频静音|无属性值|无|^|
|`autoplay`|设置音频自动播放|无属性值|若该网页的**媒体参与度比较高**的话(可以理解为用户在同一网页播放该音频的频率过高)会播放音频|^|
|`loop`|音频循环播放|无属性值|无|^|
|`preload`|设置音频预加载|1.`none`:不对音频做任何预加载处理<br>2.`metadata`:仅获取音频的基本信息(如播放时长等)，但有时也会预加载一部分内容<br>3.`auto`:预加载整个音频文件，即使用户不希望这样做|无|^|

---

#### ⑨全局属性

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`contenteditable`|表示元素是否可被用户编辑|1.`true`:可编辑<br>2.`false`:不可编辑|无|
|`draggable`|表示元素是否可被用户拖拽|1.`true`:可拖拽<br>2.`false`:不可拖拽|一般与`JS`搭配使用|
|`hidden`|隐藏元素|无属性值|无|
|`spellcheck`|规定是否对元素进行拼写和语法检查|1.`true`:检查<br>2.`false`:不检查|需要将浏览器的拼写检查开关打开才能使用|
|`contextmenu`|规定元素的上下文菜单，在用户鼠标右键点击元素时显示|无属性值|无|
|`data-*`|用于存储页面的私有定制数据|数据|无|

---

#### ⑩兼容性

1. 添加元信息，让浏览器处于最优渲染模式:
~~~html
    <!--设置IE总是使用最新的文档模式进行渲染-->
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <!--优先使用 webkit ( Chromium ) 内核进行渲染, 针对360等壳浏览器-->
    <meta name="renderer" content="webkit">
~~~
2. 使用`html5shiv`让低版本浏览器认识 H5 的语义化标签
~~~html
    <!--[if lt ie 9]>
    <script src="../../sources/js/html5shiv.js"></script>
    <![endif]-->
~~~
+ 拓展:
> `lt`:小于
> `lte`:小于等于
> `gt`:大于
> `gte`:大于等于
> `!`:非运算
+ 示例:
~~~html
    <!--[if IE 8]>仅IE8可见<![endif]-->
    <!--[if gt IE 8]>仅IE8以上可见<![endif]-->
    <!--[if lt IE 8]>仅IE8以下可见<![endif]-->
    <!--[if gte IE 8]>IE8及以上可见<![endif]-->
    <!--[if lte IE 8]>IE8及以下可见<![endif]-->
    <!--[if !IE 8]>非IE8的IE可见<![endif]-->
~~~


# 第二部分:CSS

## 一、CSS简介

> `CSS`的全称为：层叠样式表(`Cascading Style Sheets`)。
> `CSS`是一种标记语言，用于给`HTML`结构设置样式，例如：文字大小、颜色、元素宽高等等。

---

## 二、CSS语法规范

> + CSS样式中的选择器**严格区分大小写**，属性和值不区分大小写，按照书写习惯一般将“选择器、属性和值”都采用**小写**的方式。\
> + 多个属性之间必须用英文状态下的**分号**隔开，最后一个属性后的分号可以省略，但是，为了便于增加新样式最好保留。
> + 每个样式之间遵循`名:值;`的语法
> + 如果属性的值由多个单词组成且中间包含空格，则必须为这个属性值加上英文状态下的**引号**.
> + 在编写CSS代码时，为了提高代码的可读性，通常会**加上CSS注释**。
> + 在CSS代码中**空格是不被解析**的，**花括号以及分号前后的空格可有可无**。





## 三、CSS样式

### （一）行内样式(行内式/内联式)

+ 直接在标签内使用`style`属性进行CSS样式调整
+ 其属性内部CSS语法为:`名:值;`格式
+ 由于是通过属性指定的样式，因此该样式仅作用于此标签
+ 存在的问题:
  1. 书写繁琐
  2. 样式不能复用
  3. 未体现出结构与样式分离的思想
  4. 不推荐大量使用，只有对当前元素添加简单样式时，才偶尔使用

---

### （二）内部样式(内嵌式)

+ 写在`html`页面内部，将所有CSS代码提取出来，放在head标签内的style标签内
+ 语法:
~~~css
h3{
    color: green;
    font-size: 60px;
}
~~~
+ 优点:
  > 1. 理论上来说`<style>`标签可以放在HTML文档的任何地方，但一般都放在`<head>`标签中。
  > 2. 此种写法：样式可以复用、代码结构清晰。
+ 缺点:
  > 1. 并没有实现：结构与样式完全分离。
  > 2. 多个 HTML 页面无法复用样式。

---

### （三）外部样式(链入式)

+ 写在单独的`.css`文件中，随后在HTML文件中引入使用。
+ 引用时使用`link`标签，将其写在`head`标签内:`<link rel="stylesheet" href="../../CSS/CSS_style.css">`
+ 注意:
>   1. `<link>`标签属性说明：
>       + `href`：引入的文档来自于哪里
>       + `rel`:说明引入的文档与当前文档之间的关系。

>   2. 外部样式的优势：
>      1. 样式可以复用
>      2. 结构清晰
>      3. 可触发浏览器缓存机制，提高访问速度
>      4. 实现了结构与样式的完全分离

>   3. 推荐使用此样式来调用`CSS`

---

### （四）样式表优先级

+ 样式表的优先级为:**行内样式(行内式/内联式)**>**内部样式(内嵌式)**==**外部样式(链入式)**
> 1. 如果出现作用效果冲突的不同样式表，浏览器会根据优先级进行显示，如果二者优先级相同，那么写在后面的效果会将写在前面的效果覆盖掉
> 2. 对于一个样式表中出现了冲突的同种效果，后面的会覆盖前面的效果

|样式|优点|缺点|作用范围|备注|实例
|:---:|:---:|:---:|:---:|:---:|:---:|
|行内样式|优先级最高|1.样式与结构未分离<br>2.代码结构混乱<br>3.未体现出结构与样式分离的思想|style属性所在标签|无|查看[网页样例](../笔记代码/源码/HTML+CSS/CSS_样式.html)|
|内部样式|1.样式可复用<br>2.代码结构清晰|1.结构与样式未彻底分离<br>2.样式不能多页面复用|当前页面|无|^|
|外部样式|1.样式可多页面复用<br>2.代码结构清晰<br>3.可触发浏览器缓存机制，提高访问速度<br>4.实现了结构与样式的完全分离|需要引入才能使用|多个页面|无|^|

---

### （五）注释与代码风格

> 1. 注释:
>    + 格式:`/* 注释内容  */`
>    + [实例](../笔记代码/源码/HTML+CSS/CSS_样式.html)

> 2. 代码风格:
>    + CSS代码有两种风格，即开发时用的展开风格与项目上线时的紧凑风格
>    + 展开风格便于开发者进行调试与维护
>    + 紧凑风格可以减小文件体积，节约网络流量，同时能让用户打开网页时速度更快
   + 风格示例:
   + ~~~css
        /*展开风格*/
        h1 {
          color: red;
          font-size: 20px;
        }
        /*紧凑风格*/
        h1{color:red;font-size:20px;}
     ~~~
+ 项目上线时，可以通过工具将“展开风格”的代码，转换成“紧凑风格”。这样可以减小文件体积，节约网络流量，同时能让用户打开网页时速度更快
---

## 四、选择器

### （一）基本选择器

> + 基本选择器有四种，分别为:
>   1. 通配选择器
>   2. 元素选择器(标记选择器)
>   3. 类选择器
>   4. id选择器

|选择器|特点|
|:---:|:---:|
|通配选择器|选中所有标签，一般用于清除样式。|
|元素选择器(标记选择器)|选中所有同种标签，但是不能差异化选择。|
|类选择器|选中所有特定类名（ class 值）的元素|
|ID 选择器|选中特定 id 值的那个元素（唯一的）|

#### ①通配选择器

+ 通配选择器可以作用于HTML的所有元素
+ 示例:
~~~css
    /*使用符号 '*' 来使用通配选择器*/
    * {
        color: red;
    }
~~~

---

#### ②元素选择器(标记选择器)

+ 元素选择器(标记选择器)可以为页面中某一元素(标签)统一设置样式
+ 示例:
~~~css
    /*直接使用标签名来作用在为特定标签设置样式*/
    h1 {
        color: red;
    }
    /*元素选择器(标记选择器)会将全部的对应标签都设置成统一样式，无法实现差异化设置*/
~~~

---

#### ③类选择器

+ 根据元素的 class 值，来选中某些元素
+ 注意:
> + 类选择器使用“.”（英文点号）进行标识，后面紧跟类名。**元素的class属性值不带'.'，但 CSS 的类选择器要带'.'**。
> + 格式为:`.类名{属性1:属性值1; 属性2:属性值2; 属性3:属性值3; }`
> + class 值，是我们自定义的，按照标准：**不要使用纯数字、不要使用中文、尽量使用英文与数字的组合**，若由多个单词组成，使用 - 做连接，例如： left-menu ，且命名要有意义，做到 “见名知意”。
> + 一个元素的 class 属性，能写多个值，要**用空格隔开**
+ [示例](../笔记代码/源码/HTML+CSS/CSS_类选择器.html)

---

#### ④ID选择器

+ ID选择器可以根据元素的 id 属性值，来精准的选中某个元素
+ 注意:
> 1. 一个元素只能有一个id属性，不同元素间的id值不同
> 2. 一个元素可以同时拥有 id 和 class 属性
> 3. `id`属性值：**尽量由字母、数字、下划线( _ )、短杠( - )组成，最好以字母开头、不要包含空格、区分大小写**。
+ 格式为:`#id{属性1: 属性值1; 属性2: 属性值2; ....}`
+ [示例](../笔记代码/源码/HTML+CSS/CSS_ID选择器.html)

---

### （二）复合选择器

#### ①HTML元素关系

> + HTML元素间关系可以分为:
>   1. 父元素:直接包裹某个元素的元素
>   2. 子元素:被父元素直接包含的元素
>   3. 祖先元素:父亲的父亲......，一直往外找，都是祖先;父元素也算祖先元素
>   4. 后代元素:儿子的儿子......，一直往里找，都是后代;子元素，也算后代元素
>   5. 兄弟元素:具有相同父元素的元素，互为兄弟元素

---

#### ②交集选择器

+ 交集选择器用于**选中同时符合多个条件的元素**
+ 语法:`选择器1选择器2选择器3...{}`
+ 注意:
>  1. 有标签名，标签名必须写在前面。
>  2. id 选择器、通配选择器，理论上可以作为交集的条件，但实际应用中几乎不用,因为没有意义
>  3. 用的最多的交集选择器是：元素选择器配合类名选择器，例如： `p.test1{color: red;}`
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_交集选择器.html):
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>test</title>
    <style>
        .test1{
            color: blue;
        }
        .test2{
            color: red;
        }
        /* 对类名包括test1与test2的元素设置样式 */
        .test1.test2{
            color: green;
        }
        .changecolor{
            color: yellow;
        }
        /* 对使用了类名为changecolor的p标签进行样式设置*/
        p.changecolor{
            color: brown;
        }
        
    </style>
</head>
<body>
    <p class="changecolor">测试1</p> <!-- 满足使用类名为changecolor的p标签，颜色为棕色 -->
    <h2 class="changecolor">测试2</h2>  <!-- 使用类选择器进行样式设置，颜色为黄色 -->
    <p class="test1 test2">测试3</p>  <!-- 满足类名包括test1与test2的标签，颜色为绿色；此处若将交集选择器注释掉，将显示红色，因为在<style>标签中，test2在test1之后被写，因此test2会将与test1冲突的属性覆盖 -->
    <h2 class="test1">测试4</h2>  <!-- 使用类选择器进行样式设置，颜色为蓝色 -->
    <h2 class="test2">测试5</h2>  <!-- 使用类选择器进行样式设置，颜色为红色 -->
</body>
</html>
~~~

---

#### ③并集选择器

+ 并集选择器用于**选中多个选择器对应的元素**，又称：分组选择器
+ 语法:`选择器1.选择器2,选择器3....{}`
+ 注意:
> 1. 对于并集选择器，一般写一个选择器就要换一行，竖着写
> 2. 任何形式的选择器，都可以作为并集选择器的一部分 。
> 3. 并集选择器，通常用于集体声明，可以缩小样式表体积
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_并集选择器.html):
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS_并集选择器</title>
    <style>
        .green{
            color: green;
        }
        .red{
            color: red;
        }
        .blue{
            color: blue;
        }
        /*使用并集选择器时，每隔一个选择器换一行，竖着写，不然看着乱*/
        /*并集选择器支持任意选择器，且没有特定顺序*/
        .green,
        .red,
        .blue,
        #test4,
        span{
            font-size: 40px;
            background-color: brown;
            width: 200px;
        }
    </style>
</head>
<body>
    <h2 class="green">测试1</h2>
    <h2 class="red">测试2</h2>
    <h2 class="blue">测试3</h2>
    <p id="test4">测试4</p>
    <span>测试5</span>
</body>
</html>
~~~

---

#### ④后代选择器

+ 选中指定元素中，符合要求的后代元素
+ 语法:`选择器1 选择器2 选择器3 ....{}`
+ 注意:
> 1. 后代选择器，最终**选择的是后代**，不选中祖先
> 2. 儿子及之后的元素，都算是后代
> 3. 结构一定要符合之前讲的`HTML`嵌套要求，例如：不能在`p`中写`h1`~`h6`(`p`标签不能嵌套块元素)
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_后代选择器.html):
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS_后代选择器</title>
    <style>
        /*该选择器作用于所有后代元素有li的ul标签*/
        ul li{
            color: blue;
            font-size: 40px;
        }
        /*直接使用类名进行后代选择，同时在里面写其它复合选择*/
        .day li.noon{
            color: red;
            font-size: 30px;
        }
        .day div.noon{
            color: green;
            font-size: 30px;
        }
    </style>
</head>
<body>
    <ul>
        <li>河北</li>
        <li>上海</li>
        <li>天津</li>
        <li>广东</li>
        <div>
            <li>福建</li> <!-- 这个li标签并不是ul的直接子元素，但仍然为ul的后代元素，因此后代选择器依然有效 -->
        </div>
        <li>北京</li>
    </ul>
    <br>
    <ol class="day">
        <li>早上睡觉</li>
        <li class="noon">中午打游戏</li>
        <li>
            <span>晚上摆烂</span>
        </li>
        <div class="noon">
            测试
        </div>
    </ol>
</body>
</html>
~~~

---

#### ⑤子代选择器

+ 子代选择器可以选中指定元素中，符合要求的子元素
+ 语法:`选择器1>选择器2>选择器3....{}`
+ 注意:
> +  子代选择器最终选择的是子代，也只能是子代，不会选择父代或子代之后的后代元素
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_子代选择器.html)
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS_子代选择器</title>
    <style>
        div>a{
            color: red;
        }
        div>p>a{
            color: green;
        }
        /*以下三种写法都可以让最后的a标签内的文本变色*/
        /*该写法直接通过类选择器先选出嵌套对应a的div元素再进行子代选择*/
        .end>a{
            color: brown;
        }
        /*该写法中的div指嵌套对应的a的div*/
        div.end>a{
            color: brown;
        }
        /*该写法中的div指的是最外层的div*/
        div .end>a{
            color: brown;
        }
    </style>
</head>
<body>
    <div>
        <a href="#">张三</a>
        <a href="#">李四</a>
        <a href="#">王五</a>
        <p>
            <a href="#">赵六</a> <!-- 对于这个a，由于它不是任何div元素的子元素，因此'div>a'的css选择器无法作用于它 -->
        </p>
        <div class="end">
            <a href="#">孙七</a>
        </div>
    </div>
</body>
</html>
~~~

---

#### ⑥兄弟选择器

+ 兄弟选择器分两类，一种为**相邻兄弟选择器**，另一种为**通用兄弟选择器**
  1. 相邻兄弟选择器:选中指定元素后，符合条件的**相邻兄弟**元素
     + 语法:`选择器1+选择器2{}`
  2. 通用兄弟选择器:选中指定元素后，符合条件的**所有兄弟**元素
     + 语法:`选择器1~选择器2{}`
> 注意:两种选择器都**只能选中下面的兄弟**，而不能选中上面的
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_兄弟选择器.html):
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS_兄弟选择器</title>
    <style>
        /*使用相邻兄弟选择器，只会选择和div相邻的下方的p元素*/
        div+p{
            color: blue;
        }
        /*使用通用兄弟选择器，会选择与div同时兄弟的且在div下方的全部p元素*/
        div~p{
            color: red;
        }
        /*使用这种方式可以为除列表的第一项外其它项都设置一个统一的样式*/
        li+li{
            color: green;
        }
        /*这种写法与上方的效果一致，只是使用的兄弟选择器不同*/
        li~li{
            color: green;
        }
    </style>
</head>
<body>
    <div>一天的生活</div>
    <p>早上睡觉</p>
    <p>中午吃个饭</p>
    <p>下午玩会</p>
    <p>晚上emo</p>
    <ul>
        <li>省份</li>
        <li>河北</li>
        <li>山东</li>
        <li>河南</li>
        <li>山西</li>
    </ul>
</body>
</html>
~~~

---

#### ⑦属性选择器

+ 属性选择器可以选中属性值符合一定要求的元素
+ 属性选择器共有五种格式:
  1. `[属性名]{}`选中具有某个属性的元素
  2. `[属性名="值"]`选中包含某个属性，且属性值**等于**指定值的元素
  3. `[属性名^="值"]`选中包含某个属性，且属性值以指定的值**开头**的元素
  4. `[属性名$="值"]`选中包含某个属性，且属性值以指定的值**结尾**的元素
  5. `[属性名*="值"]`选择包含某个属性，属性值**包含**指定值的元素
+ [网页示例](../笔记代码/源码/HTML+CSS/CSS_属性选择器.html):
~~~html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>CSS_属性选择器</title>
    <style>
        [title]{
            color: red;
        }
        [title="测试1"]{
            color: green;
        }
        [title^="测试2"]{
            color: blanchedalmond;
        }
        [title$="t1"]{
            color: blue;
        }
        [title*="t2"]{
            color: aqua;
        }
    </style>
</head>
<body>
    <div title="测试1">测试1</div>
    <div title="测试2">测试2</div>
    <div title="test1">测试3</div>
    <div title="test2">测试4</div>
    <div title="测5">测试5</div>
</body>
</html>
~~~

---

#### ⑧伪类选择器

##### Ⅰ简要概述

+ 伪类选择器用于选中**处于特殊状态**的元素
+ “伪类”，就是像类，但不是类，而是元素的一种特殊状态。

---

##### Ⅱ动态伪类

|伪类|含义|备注|样例|
|:---:|:---:|:---:|:---:|
|`:link`|超链接**未被访问**的状态|1.该伪类**仅适用于超链接**<br>2.在超链接的四个常用伪类里，它需要**写在最前面**|[网页样例](../笔记代码/源码/HTML+CSS/CSS_伪类选择器_动态伪类.html)|
|`:visited`|超链接**访问过**的状态|1.该伪类**仅适用于超链接**<br>2.在超链接的四个常用伪类里，它需要**写在第二个**|^|
|`:hover`|鼠标**悬停**在元素上的状态|1.该伪类适用于全部标签<br>2.在超链接的四个常用伪类里，它需要**写在第三个**|^|
|`:active`|鼠标**按下**时元素的状态|1.该伪类适用于全部标签<br>2.在超链接的四个常用伪类里，它需要**写在最后**|^|
|`:focus`|**获取焦点**的元素状态|该伪类**适用于表单标签**|

---

##### Ⅲ结构伪类

|伪类|含义|详解|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`:first-child`|在对应元素的**所有兄弟元素**中选择第一个|需要满足两个条件:<br>1.与其所有兄弟元素相比，它是第一个元素<br>2.它符合伪类左边的筛选条件|无|[结构样例1](../笔记代码/源码/HTML+CSS/CSS_伪类_结构伪类(1).html)
|`:last-child`|在对应元素的**所有兄弟元素**中选择最后一个|需要满足两个条件:<br>1.与其所有兄弟元素相比，它是最后一个元素<br>2.它符合伪类左边的筛选条件|使用方法与`:first-child`相同|^|
|`:nth-child(n)`|在对应元素的**所有兄弟元素**中选择第n个|`n`的格式为`an+b`的形式，其中**n>=0**|<a href="#nth">n值备注</a>|[结构样例2](../笔记代码/源码/HTML+CSS/CSS_伪类_结构伪类(2).html)|
|`:firstt-of-type`|在对应元素的**所有同类型兄弟元素**中选择第一个|需要满足两个条件:<br>1.与其所有同类型兄弟元素相比，它是第一个元素<br>2.它符合伪类左边的筛选条件|无|[结构样例3](../笔记代码/源码/HTML+CSS/CSS_伪类_结构伪类(3).html)|
|`last-of-type`|在对应元素的**所有同类型兄弟元素**中选择最后一个|需要满足两个条件:<br>1.与其所有同类型兄弟元素相比，它是最后一个元素<br>2.它符合伪类左边的筛选条件|使用方法与`:firstt-of-type`相同|^|
|`:nth-of-type(n)`|在对应元素的**所有同类型兄弟元素**中选择第n个|`n`的格式为`an+b`的形式，其中**n>=0**|<a href="#nth">n值备注</a>|^|
|`:nth-last-child(n)`|在对应元素的**所有兄弟元素**中选择倒数第n个|^|^|[结构样例4](../笔记代码/源码/HTML+CSS/CSS_伪类_结构伪类(4).html)|
|`:nth-last-of-type(n)`|在对应元素的**所有同类型兄弟元素**中选择倒数第n个|^|^|^|
|`:only-child`|选择**没有兄弟元素**的元素|需要满足两个条件:<br>1.没有兄弟元素<br>2.它符合伪类左边的筛选条件|无|^|
|`:only-of-type`|选择**没有同类型兄弟元素**的元素|需要满足两个条件:<br>1.没有同类型兄弟元素<br>2.它符合伪类左边的筛选条件|^|^|
|`:root`|选择**根元素**|即html标签|无|^|
|`:empty`|选择**内容为空**的元素|选择只写了标签没写内容的元素|写了空格等空白字符也算有内容|^|

<a id="nth"></a>

|值|解释|备注|
|:---:|:---:|:---:|
|`0`|什么都不选中|公式需要满足`an+b`的格式|
|`空值`|^|^|
|`1~正无穷的整数`|选中对应序号的子元素|^|
|`2n 或 even`|选中序号为偶数的子元素|^|
|`2n+1 或 odd`|选中序号为奇数的子元素|^|
|`-n+3`|选中的是前 3 个|^|

---

##### Ⅳ否定伪类

+ `:not(选择器)`选择排除了满足括号条件中的元素
+ [否定伪类样例](../笔记代码/源码/HTML+CSS/CSS_伪类_否定伪类.html)

---

##### ⅤUI伪类

|伪类|含义|备注|样例|
|:---:|:---:|:---:|:---:|
|`:checked`|选择**被选中的**元素|一般用于单选框或复选框|[UI伪类样例](../笔记代码/源码/HTML+CSS/CSS_伪类_UI伪类.html)|
|`:enable`|选择**可用的**表单元素|即选择没有disabled属性的表单元素|^|
|`:disabled`|选择**不可用的**表单元素|无|^|

---

##### Ⅵ目标伪类

+ `:target`可以选择**锚点指向**的元素
+ [目标伪类样例](../笔记代码/源码/HTML+CSS/CSS_伪类_目标伪类.html)

---

##### Ⅶ语言伪类

+ `:lang(语言)`可以根据**指定的语言**选择对应元素(其本质是看目标元素的属性lang值是否是该语言)
+ [语言伪类样例](../笔记代码/源码/HTML+CSS/CSS_伪类_语言伪类.html)

---

#### ⑨伪元素选择器

> + 伪元素指像元素，但不是元素，而是元素中的一些特殊位置
+ 常用伪元素:

|伪元素|含义|备注|通用备注|示例|
|:---:|:---:|:---:|:---:|:---:|
|`::first-letter`|选中元素中的**第一个文字**|无|有些伪元素使用一个引号与两个引号都可以，但有些伪元素只能用两个引号来使用，原因是**在CSS2时被提出的伪元素并没有与伪类做太大区分，因此使用一个引号和两个引号都可以，但CSS3提出的伪元素被要求必须使用两个引号与伪类相区分**|[伪元素示例](../笔记代码/源码/HTML+CSS/CSS_伪元素选择器.html)
|`::first-line`|选中元素中的**第一行文字**|无|^|^|
|`::selection`|选中**被鼠标选中**的内容|无|^|^|
|`::placeholder`|选中输入框中placeholder属性中的内容|无|^|^|
|`::before`|在元素的内容**前面**插入新内容|是在元素内容前面插入，不是在元素标签前面插入|^|^|
|`::after`|在元素的内容**后面**插入新内容|是在元素内容后面插入，不是在元素标签后面插入|^|^|

---

### （三）选择器优先级

+ 如果**不同的选择器**在选中了页面**相同的元素**后，都有对这个元素**相同的属性**有**不同的样式设置**，即出现了冲突，那么就需要考虑选择器的优先级了

#### ①基本选择器优先级

> + 行内>ID选择器>类选择器>元素选择器>通配选择器
> + 若同种类型选择器属性冲突，则浏览器会按照**后来者居上**的原则进行显示

---

<a id="priority"></a>

#### ②复合选择器优先级

+ 在计算复合选择器的优先级时，会根据权重的大小选择优先级，其权重的格式为:`(a,b,c)`
> + 其中:
>   1. `a`:ID选择器的个数
>   2. `b`:类、伪类、属性选择器的个数
>   3. `c`:元素、伪元素选择器的个数

+ 比较规则:按照**从左至右**，即从`a`到`c`的顺序依次比较权重值的大小，**当前位胜出后，后位便不会再比较大小而会直接决定优先级**。如果二者权重完全一致，则浏览器按照**后来者居上**的原则显示
+ 例:
> + `(1,0,0)`>`0,2,2`
> + `(1,1,0)`>` (1,0,3)`
> + `(1,1,3)`>`(1,1,2)`
+ 特殊规则:
> 1. **行内样式**权重大于任何选择器权重
> 2. **并集选择器**实际上是**每一个部分分开来计算**的! [并集选择器优先级样例](../笔记代码/源码/HTML+CSS/CSS_并集选择器优先级.html)
> 3. 实际上，**基本选择器的优先级也是按照此规则来进行比较的**
> 4. `!import`的权重大于行内样式，也大于任意选择器，是**权重最高**的
> 5. 除非在不得已的情况下，**不建议使用**`!important`

---

## 五、CSS三大特性

### （一）层叠性

> + 如果发生了**样式冲突**，那么浏览器会**根据一定的规则**，进行**样式的层叠**(即覆盖)
> + 层叠性就是为了解决样式冲突问题的
> + 样式冲突:元素的**同一个样式名**，被设置成了**不同的样式值**

---

### （二）继承性

> + 子代元素会自**动继承父代或祖先元素**设置的**某些样式**
> + 继承的规则:如果出现了样式冲突，优先继承父代或祖先元素里**最近的**样式
> + 字体属性、文本属性(除`vertical-align`)、文字颜色等都可以继承，但边框、背景、外边距、宽高、溢出处理方式等无法被继承。
> + 规律:能继承的属性，**都是不影响布局的**，或者说，**都是与盒子模型没有关系的**
> + [详情参考MDN文档](https://developer.mozilla.org/zh-CN/)

---

### （三）优先级

> + `!important`>行内样式>ID选择器>类选择器>元素选择器>通配选择器>继承样式
> + 详细优先级参考<a href="#priority">复合选择器优先级</a>

---

## 六、CSS常用属性

<a id="css_length"></a>

### （一）长度单位

|格式|单位|说明|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|像素值|`px`|电脑屏幕是由一个个小点构成的，每一个点就是一像素|无|[长度单位样例](../笔记代码/源码/HTML+CSS/CSS_长度单位.html)|
|相对长度单位|`em`|`em`是相对于当前元素的`font-size`值来进行样式调整的，其计算方法为:`实际大小=em值*font-size值(单位:px)`|如果当前元素没有设置`font-size`，则会向其最近的祖先元素寻找，若仍没有继续向上寻找，若全都没有，使用浏览器默认字体大小(一般为16px)|^|
|^|`rem`|`rem`是相对于根元素(即HTML)的`font-size`值来进行样式调整的，计算方法与`em`一致|如果根元素没有设置`font-size`，则会使用浏览器默认字体大小(一般为16px)|^|
|百分比|无单位|使用百分比进行调整，是相对于父元素的对应属性进行调整的|无|^|


#### ①像素值

+ 电脑屏幕是由一个个小点构成的，每一个点就是一像素
+ 像素点**越小**，呈现出来的效果就**越清晰、细腻**
+ 在CSS中，一般**使用像素**(`px`)作元素宽高等属性调整的单位，日常生活中的`cm`和`mm`也可以用,但几乎不用，因为**和像素相比，它们跨度太大，不够精细**。

---

#### ②em和rem

+ `em`和`rem`是相对长度单位
  + `em`是相对于当前元素的`font-size`值来进行样式调整的，其计算方法为:`实际大小=em值*font-size值(单位:px)`
    + 如果当前元素没有设置`font-size`，则会向其最近的祖先元素寻找，若仍没有继续向上寻找，若全都没有，使用浏览器默认字体大小(一般为16px)
  + `rem`是相对于根元素(即HTML)的`font-size`值来进行样式调整的，计算方法与`em`一致
    + 如果根元素没有设置`font-size`，则会使用浏览器默认字体大小(一般为16px)

---

#### ③百分比

+ 使用百分比进行调整，是相对于父元素的对应属性进行调整的

---

<a id="css_color"></a>

### （二）颜色

#### ①颜色名表示

+ 直接使用颜色对应的英文单词，编写比较简单
+ 颜色名并不能表示全部的颜色，只能表达特定颜色，因此使用不多
+ 具体颜色名参考[MDN_CSS颜色文档](https://developer.mozilla.org/en-US/docs/Web/CSS/named-color)

---

#### ②RGB与RGBA

+ RGB使用光的三原色(红色、绿色、蓝色)来进行组合表现出各种不同的颜色
+ RGBA在RGB的基础上添加了透明度
+ 注意:
> 1. 对于RGB和RGBA来说，前三位要么全是**0~255的整数**，要么全是**百分比形式**的数，总之要让他们的格式保持一致。对于RGBA的透明度设置，可以**在规范范围内随意写**
> 2. `rgb(0,0,0)`为黑色,`rgb(255,255,255)`为白色
> 3. 若三种颜色值**相同**，则会呈现**灰色**，值越大灰色越深
+ [RGB/RGBA样例](../笔记代码/源码/HTML+CSS/CSS_颜色_RGB与RGBA.html)

---

#### ③HEX与HEXA

+ `Hex`的原理与`rbg`一样，依然通过光的三原色来进行颜色组合，只是需要用六位(三组)来表示
+ `HEXA`也是在`HEX`的基础上添加了透明度
+ 格式为:`color: #rrbbgg;`
> + 每一位的取值范围为`0~f`，即16进制数。因此每一种光的最小值为00，最大值为ff
> + 如果三种颜色的光两位都是相同的，可以简写成一位，如:`color: #abc`，等价于`color: #aabbcc`
> + 如果前三组都简写了，透明度也必须简写
> + IE浏览器支持`HEX`但不支持`HEXA`
+ [HEX/HEXA样例](../笔记代码/源码/HTML+CSS/CSS_颜色_HEX与HEXA.html)

---

#### ④HSL和HSLA

+ `HSL`通过`色相`、`饱和度`、`亮度`来表示颜色
+ 格式为:`color: hsl(色相(度数或整数值),饱和度(百分比),亮度(百分比))`
> 1. 色相范围为0°~360°，详情查看[W3school-HSL颜色](https://www.w3school.com.cn/css/css_colors_hsl.asp)
> 2. 饱和度使用百分比表示，它反映了颜色偏灰的程度,值为`0%`时全灰，值为`100%`无效果
> 3. 亮度使用百分比表示，它反映了颜色偏黑或者偏白，值为`0%`全黑,值为`100%`全白
+ `HSLA`在`HSL`的基础上添加了透明度
+ [HSL/HSLA样例](../笔记代码/源码/HTML+CSS/CSS_颜色_HSL和HSLA.html)

---

### （三）字体

#### ①样式表

|样式|含义|值|备注|
|:---:|:---:|:---:|:---:|
|`font-size`|调节字体大小|数值|无|
|`font-family`|调节字体类型|字体类型|写值时建议使用引号，且可以写入多个值|
|`font-style`|调节字体是否为斜体|1.`normal`:正常(默认)<br>2.`italic`:会首先检索该字体是否自带斜体，若自带使用该斜体，若不自带强制倾斜<br>3.`oblique`:直接强制让字体变斜|无|
`font-weight`|调节字体粗细程度|1.关键词:包括`lighter`、`normal`、`bold`、`bolder`<br>2.数值:范围为`100~1000`，一般是可以被100整除的整数|该样式的效果最终取决于字体对于粗细设计的精细程度|
|`font`|复合属性|以上属性值|详情参考[字体复合属性详解](#font)|


#### ①字体大小

+ 字体大小通过属性名`font-size`来调节
+ 注意:
> 1. `chrome`浏览器和`edge`浏览器**默认支持的最小字体**为12px，若比此大小还小，则字体**依旧**会以12px的大小显示。
> 2. `chrome`浏览器和`edge`浏览器**默认的字体大小**为16px
> 3. 字体大小的设置在`设置`->`外观`->`自定义字体`中可以调节
> 4. 不同浏览器支持的最小字体大小和默认字体大小可能**不同**，最好使用样式调节字体大小而不要用浏览器默认字体大小
> 5. 通常可以用元素选择器对`body`元素进行`font-size`的样式设置，这样可以通过继承样式的方法调节各元素的字体大小
+ 补充:
> 1. `font-size`实际上是**作用于文字的字体设计框的高度**的，且其宽度会随其高度调整而自适应。而由于不同字体类型的同一文字在框内显示的大小不同，就会导致**文字最终呈现的大小，并不一定与`font-size`设定的值一致，可能偏大也可能偏小**
> 2. 大多数的字体设计时，其文字相对于字体设计框，都靠下一些。因此**在网页上显示时，文字也通常会靠下一些**


---

#### ②字体族

+ 字体族(`font-family`)可以**控制字体的类型**
+ 注意:
> 1. 如果字体名包含空格，必须用引号将字体名包起来
> 2. 可以设置多个字体，浏览器在设置样式时会从左至右依次寻找支持的字体，如果找到就用，没有找到就是用后面的，全没有找到会使用默认字体
> 3. 设置多个字体时，通常在最后面加上`serif`(衬线字体:字体棱角分明)或`sans-serif`(非衬线字体:字体棱角不分明)，以告知浏览器寻找可用的衬线/非衬线字体设置样式
> 4. `Windows`系统的默认字体为`微软雅黑`
+ [字体族样例](../笔记代码/源码/HTML+CSS/CSS_字体_字体族.html)

---

#### ③字体倾斜与粗细

+ `font-style`和`font-weight`分别可以调节**字体是否倾斜**与**字体粗细程度**
  1. `font-style`有三个常用值
     + `normal`：正常(默认)
     + `italic`:会首先检索该字体是否自带斜体，若自带使用该斜体，若不自带强制倾斜
     + `oblique`:直接强制让字体变斜
   2. `font-weight`可以写**关键词**，也可以写**数值**
      1. 关键词(**关键词是否生效取决于字体是否设计了该粗细效果**):
         + `lighter`:细
         + `normal`:正常
         + `bold`:加粗
         + `bolder`(**大部分字体没有设计此情况，因此可能依旧会显示`bold`效果**):更粗
       2. 数值:
          + 数值的取值范围是`100~1000`，一般取能被100整除的整数，数值效果取决于**字体设计时其粗细设计的精细程度**
          + 一般来说`100~300`的值对应`lighter`效果,`400~600`对应`normal`效果,`600`及以上对应`bold`效果

---

<a id="font"></a>

#### ④字体复合属性

+ 字体复合属性(`font`)用于对字体样式进行**综合设置**
+ 编写规则:
> 1. 必须包括字体大小(`font-size`)和字体族(`font-family`)
> 2. 字体族(`font-family`)必须在最后一位，字体大小(`font-size`)必须在倒数第二位
> 3. 各个属性间用空格隔开
+ **复合属性如果没有设置相关属性,会自动将未设置的属性设为默认值并覆盖前面的冲突属性**

[字体复合属性样例](../笔记代码/源码/HTML+CSS/CSS_字体_字体复合属性.html)

---

### （四）文本

#### ①文本样式表

|样式|含义|值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`color`|设置文本颜色|详情参考[颜色部分笔记](#css_color)|无|[文本颜色与间距样例](../笔记代码/源码/HTML+CSS/CSS_文本_文本颜色与间距.html)|
|`letter-spacing`|设置文本各字符之间的间距|像素值|可以写负值，负值即间距缩小|^|
|`word-spacing`|设置文本各单词之间的间距|像素值|1.可以写负值，负值即间距缩小<br>2.浏览器通过识别空格来判断单词|^|
|`text-decoration`|控制文本的各种装饰线|[详情见文本修饰详解](#text-decoration)|使用多个属性时，用空格隔开。且无特定顺序|[文本修饰样例](../笔记代码/源码/HTML+CSS/CSS_文本_文本修饰.html)|
|`text-indent`|首行缩进|CSS的长度单位|无|无|
|`text-align`|设置文本水平对齐方式|  1. `left`：左对齐<br>2. `center`：居中对齐<br>3. `right`：右对齐|无|^|
|`line-height`|设置行高|1.像素值<br>2.数值(小数或整数)<br>3.百分数<br>4.`normal`:默认值|无|[行高样例](../笔记代码/源码/HTML+CSS/CSS_文本_行高.html)|
|`vertical-align`|作用于**指定一行元素间**或**表格单元格**之间的垂直对齐方式|1. `top`:使元素的顶部与其所在行的顶部对齐<br>2. `baseline`:默认值，使元素的基线与父元素的基线对齐<br>3. `middle`:使元素的中部与父元素的基线加上父元素字母x高度的一半对齐(即元素的中部与父元素字母x(小写)的中部对其).**它不意味着让文本显示在垂直正中位置**<br>4. `bottom`:使元素的底部与其所在行的底部对齐|1.`vertical-align`不能作用于块元素<br>2.若`vertical-align`的作用对象负责支撑父元素的盒子，则使用`vertical-align`会**使其它文本或图像等以作用对象为中心移动**，而不会移动作用对象，因为该对象起支撑作用|[vertical-align样例](../笔记代码/源码/HTML+CSS/CSS_文本_垂直对齐.html)|
|`text-transform`|设置文本大小写|1. `capitalize`:设置首字母大写<br>2. `uppercase`:强制所有字符转换为大写<br>3. `lowercase`:强制所有字符转换为小写<br>4. `none`:阻止所有字符的大小写被转换,该值为默认值。|[更多](https://developer.mozilla.org/zh-CN/docs/Web/CSS/text-transform)|[其它文本样例1](../笔记代码/源码/HTML+CSS/CSS_文本_其它文本样例1.html)|

---

#### ②文本颜色与间距

+ `color`用于设定文本颜色，详情参考[颜色部分笔记](#css_color)
+ `letter-spacing`用于设置文本**各字符之间的间距**(属性)，`word-spacing`用于设置文本**各单词(浏览器通过识别空格来判断单词)之间的间距**
+ `letter-spacing`和`word-spacing`的值都为像素值
+ [文本颜色与间距样例](../笔记代码/源码/HTML+CSS/CSS_文本_文本颜色与间距.html)

---

<a id="text-decoration"></a>

#### ③文本修饰

+ `text-decoration`可以设置文本的各种**装饰线**
+ 可选值有:
  1. `none`：无装饰线
  2. `underline`：下划线
  3. `overline`：上划线
  4. `line-through`：删除线
+ `text-decoration`还可以搭配以下值使用(**各值之间用空格隔开，没有特定顺序**):
  1. `dotted`:虚线
  2. `wavy`:波浪线
  3. `solid`:默认值，单实线
  4. `double`:双实线
  5. `dashed`:虚线
  6. 颜色属性
+ [文本修饰样例](../笔记代码/源码/HTML+CSS/CSS_文本_文本修饰.html)

---

#### ④首行缩进

+ `text-indent`可以用于控制文本**首字母缩进**
+ 使用**CSS长度单位**来控制缩进范围

---

#### ⑤文本对齐

##### Ⅰ水平对齐

+ `text-align`用于控制文本的**水平对齐方式**
+ 有三个可选值:
  1. `left`：左对齐
  2. `center`：居中对齐
  3. `right`：右对齐

---

##### Ⅱ垂直对齐

1. 使用行高进行垂直对齐:
>    + 顶部对齐为默认情况
>    + 居中对齐:`line-height`=`height`
>    + 底部对齐:`line-height`=`height`*2-`font-size`-`x`(x为字体设计时普遍偏下的一些像素，大概为`10~20`px)
2. `vertical-align`也可以调整文本的垂直对齐方式:
> + `vertical-align`作用于**指定一行元素间**或**表格单元格**之间的垂直对齐方式
> + 常用值:
>   1. `top`:使元素的顶部与其所在行的顶部对齐
>   2. `baseline`:默认值，使元素的基线与父元素的基线对齐
>   3. `middle`:使元素的中部与父元素的基线加上父元素字母x高度的一半对齐(即元素的中部与父元素字母x(小写)的中部对其).**但它不意味着让文本显示在子元素垂直正中位置**
>   4. `bottom`:使元素的底部与其所在行的底部对齐
> + 注意:
>   1. `vertical-align`属性**不能作用于块元素**
>   2. 若`vertical-align`的作用对象负责支撑父元素的盒子，则使用`vertical-align`会**使其它文本或图像等以作用对象为中心移动**，而不会移动作用对象，因为该对象起支撑作用
>   3. 对于默认的基线对齐方式，如果作用元素是单纯的容器，内部没有文本的话，**它的盒子的下边框部分**会与父元素`x`的基线对齐，但若作用元素是有文本的(如里面有内容的`span`标签)，或间接有文本的(如`input`输入框，可能会被用户输入文本)，那么是**它所含的文本的下端**与父元素`x`基线对齐而不是该元素的下边框与父元素`x`基线对齐
>   4. `vertical-align`**实际上作用的是元素,而不是内部的文本**
> + [vertical-align样例](../笔记代码/源码/HTML+CSS/CSS_文本_垂直对齐.html)


---

#### ⑥行高

+ `line-height`可以**设置文本行的高度**
+ `line-height`支持多种类型的值:
>   1. 像素值
>   2. `normal`:浏览器**默认**的值，如果不写默认此效果
>   3. 百分比
>   4. 数字(可以为小数也可以为整数):
>   + 数值和百分比最终呈现的行高为:**(数值(或百分比))*(当前元素font-size属性值)px**
+ 注意:
> 1. 由于字体设计原因，文字在一行中一般并**不是**绝对垂直居中的，但不仔细看差别不大
> 2. 设置行高时应将行高设置的比`font-size`属性设置的字体大小要大些(大约为`1.5~2`倍)，如果行高的值与`font-size`的值相近，可能会出现行和文字垂直重叠的情况
> 3. 行高设为0的情况:
>    + 各行文字相互覆盖
>    + 背景色消失
>    + 文字上半部分与更上方重叠(若在网页最上方，则可以发现文字的上部分有一部分被网页遮挡了)
>    + 若将行高设为**负值**，则浏览器会判定此时行高的值**不合法**，会强制行高设置为`normal`
> 4. 行高是可以继承的，**同一行内不同大小的文字通过[字体设计框的基线](https://developer.mozilla.org/zh-CN/docs/Glossary/Baseline)对齐**。因此对于继承的文本，若其字体大小不同，**使用百分比或数字的值更能动态的调整行高**
> 5. `line-height`与`height`的关系:
>    1. **有**`height`属性时，**盒子的高度会为`height`属性的值，与`line-height`无关**，若此时各行的高度加起来比`height`还高，则高出去的部分会**向下溢出**
>    2. 若**没有**`height`而有`line-height`，那么**盒子的高度会是各行的高度总和**
>    3. 当没有`height`属性而行高的值为0时，那么`div`的高度也是0，因此没有高度的`div`无法显示背景色，这也是为什么行高为0时无背景色的原因
> 6. `line-height`的应用场景
>    1. 对于**多行文字**：控制行与行之间的距离。
>    2. 对于**单行文字**：让`height`等于`line-height`，可以实现文字垂直居中。
+ [行高样例](../笔记代码/源码/HTML+CSS/CSS_文本_行高.html)

---

### （五）列表

+ 以下属性在`ul`、`ol`、`li`中可用

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`list-style-type`|定义列表符号格式|常用属性:<br>1.`none`:不显示符号<br>2.`square`:实心方块<br>3.`disc`:圆形<br>4.`decimal`:数字<br>5.`lower-roman/alpha`:小写罗马/英文字母<br>6.`upper-roman/alpha`:大写罗马/英文字母<br>更多参考[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/CSS/list-style-type)|无|[CSS_列表属性样例](../笔记代码/源码/HTML+CSS/CSS_列表.html)|
|`list-style-position`|设置列表符号位置|1.`inside`:在`li`的里边<br>2.`outside`:在`li`的外边|无|^|
|`list-style-image`|自定义列表符号|`url("图片地址")`|**自定义列表符号优先级高于关键词定义列表符号**|^|
|`list-style`|复合属性，可一次性设置以上属性|以上属性值|1.各属性值无顺序，但需用空格隔开<br>2.**复合属性如果没有设置相关属性,会自动将未设置的属性设为默认值并覆盖前面的冲突属性**|^|

---

### （六）表格

#### ①边框属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`border-width`|设置边框宽度|CSS可用的长度单位|无|[CSS_边框属性样例](../笔记代码/源码/HTML+CSS/CSS_边框属性.html)|
|`border-color`|设置边框颜色|详情参考[颜色部分笔记](#css_color)|无|^|
|`border-style`|设置边框线类型|1.`none`:默认值<br>2.`solid`:实线<br>3.`dashed`:虚线<br>4.`dotted`:点线<br>5.`double`:双实线|1.`input`标签的默认样式中，`border-style`的默认样式为`inset`|^|
|`border`|边框复合属性|以上属性的属性值|1.无顺序要求，各属性值用空格隔开<br>2.**复合属性如果没有设置相关属性,会自动将未设置的属性设为默认值并覆盖前面的冲突属性**|^|
|以上属性适用于**全部元素**|

#### ②表格独有属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`table-layout`|设置表格的列宽度|1.`auto`:自动，列宽根据内容计算(表格默认)<br>2.`fixed`:平均分列宽|无|[表格属性样例](../笔记代码/源码/HTML+CSS/CSS_表格属性.html)|
|`border-spacing`|设置表格单元格（各边框）间距|CSS长度单位，详情见[长度笔记](#css_length)|生效的前提为**不能设置相邻单元格边框合并(即`border-collapse`属性)**|^|
|`border-collapse`|设置相邻单元格边框合并|1.`collapse`:合并<br>2.`separate`:不合并(默认)|**该属性一旦被设置，单元格间距(`border-spacing`)便不再生效**|^|
|`empty-cells`|隐藏无内容的单元格|1.`show`:显示无内容单元格(默认)<br>2.`hide`:隐藏无内容的单元格|**最好不要与设置相邻单元格边框合并(即`border-collapse`属性)一起使用**，因为此时虽然设置了也有作用，但看不出来，因为由于此时单元格合并的原因，各单元格距离为0，因此被隐藏的单元格的四个相邻单元格或表格的边框仍然存在，使其看起来没有隐藏一般|^|
|`caption-side`|设置标题位置|1.`top`:在表格上方(默认)<br>2.`bottom`:在表格下方|无|^|

---

### （七）背景

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`background-color`|设置元素背景颜色|详情查看[颜色部分笔记](#css_color)|默认值为`transparent`(透明色)|[背景样例](../笔记代码/源码/HTML+CSS/CSS_背景属性.html)|
|`background-image`|设置背景图片|`url("图片地址")`|1.若图片比元素小则默认会重复覆盖，若比元素大则仅会显示一部分，但不会溢出<br>2.该属性若与`background-color`一起使用，该属性作用优先级比`background-color`要高|^|
|`background-position`|设置背景位置|1.使用关键字设置位置:`left`、`center`、`right`指定水平位置,`top`、`center`、`bottom`指定垂直位置<br>2.使用像素值指定位置:以元素左上角，为坐标原点，设置图片左上角到原点的位置，需要写两个值，一个x值一个y值|1.若关键词仅写一个，则浏览器会自动认为另一个为居中显示<br>2.若像素值仅写一个，则浏览器会自动认为另一个为居中显示|^|
|`background`|复合属性|以上属性值|1.无顺序要求，各属性值用空格隔开<br>2.**复合属性如果没有设置相关属性,会自动将未设置的属性设为默认值并覆盖前面的冲突属性**|^|
|更多详见[MDN文档](https://developer.mozilla.org/zh-CN/docs/Web/CSS/background)|

---

### （八）鼠标相关属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`cursor`|设置鼠标光标的样式|1.`pointer`:手<br>2.`move`:移动图标<br>3.`text`:文字选择器<br>4.`crosshair`:十字架<br>5.`wait`:等待<br>6.`help`:帮助<br>[更多](https://developer.mozilla.org/zh-CN/docs/Web/CSS/cursor)|无|[鼠标样例](../笔记代码/源码/HTML+CSS/CSS_鼠标属性.html)|

---

## 七、盒子模型

### （一）元素显示模式

#### ①特点总结

|>|元素显示模式|>|>|>|特点|备注|
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
|**显示模式**|**别称**|**是否独占一行**|**宽**|**高**|**CSS**|备注|
|块元素|块级元素|在页面中**独占一行**，不会与任何元素共用一行，是从上到下排列的|默认宽度:**撑满父元素**|默认高度:由**内容**撑开|**可以**通过CSS设置宽高|无|
|行内元素|内联元素|在页面中**不独占一行**，一行中不能容纳下的行内元素，会在下一行继续从左到右排列|默认宽度：由**内容**撑开|默认高度：由**内容**撑开|**无法**通过 CSS 设置宽高|行内元素的内容取决于`font-size`所设置的大小而不取决于其子元素的大小，因此如果将`font-size`设置为`0`的话该元素将不能被子元素撑开|
行内块元素|内联块元素|^|^|^|**可以**通过CSS设置宽高|无|

---

#### ②各常用元素显示模式汇总

1. 块元素
>   1. 主体标签:`<html>`、`<body>`
>   2. 排版标签:`h1`~`h6`、`hr`、`p`、`pre`、`div`
>   3. 列表标签:`ul`、`ol`、`li`、`dl`、`dt`、`dd`
>   4. 表格标签:`table`、`tbody`、`thead`、`tfoot`、`tr`、`caption`
>   5. 表单标签:`form`、`option`
2. 行内元素
>   1. 文本标签:`br`、`em`、`strong`、`sup`、`sub`、`del`、`ins`
>   2. `a`、`label`
3. 行内块元素
>   1. 图片:`img`
>   2. 单元格:`td`、`th`
>   3. 表单组件:`input`、`textarea`、`select`、`button`
>   4. 框架标签:`iframe`

---

#### ③修改元素显示模式

+ 通过`CSS`的`display`属性可以修改对应元素的显示模式

|值|含义|备注|
|:---:|:---:|:---:|
|`block`|修改为块状元素|无|
|`inline-block`|修改为行内块元素|无|
|`inline`|修改为行内元素|无|
|`none`|隐藏该元素的显示|无|

---

### （二）盒子模型的组成部分

#### ①模型总览

![盒子模型图例](md文件所需图片/盒子模型.png)

+ `CSS`会把所有`HTML`元素看成一个盒子，所有的样式都是基于这个盒子所调整的
+ 盒子包括:
>  1. `margin`(外边距):盒子与外界的距离
>  2. `border`(边框): 盒子的边框
>  3. `padding`(内边距):紧贴内容的空白区域
>  4. `content`(内容):元素中的文本或后代元素在此显示
+ 盒子的宽(或高)=`content`+左右(或上下)`padding`+左右(或上下)`border`
+ 对于**块元素**来说如果没有设置width，那么**盒子的宽度**为`父元素的content`-`本元素左右margin`,**盒子的文本区宽度**=`父元素content`-`本元素左右margin`-`本元素左右padding`
+ `margin`不会影响到盒子的大小，但会影响盒子的位置

---

#### ②内容区content

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`width`|设置**内容区**宽度|CSS长度单位，详情见[长度笔记](#css_length)|`width`仅能设置内容区宽度，但盒子还包括`padding`和`border`|
|`min-width`|设置**内容区**最小宽度|^|一般不与`width`一起使用|
|`max-width`|设置**内容区**最大宽度|^|^|
|`height`|设置**内容区**高|^|`height`仅能设置内容区宽度，但盒子还包括`padding`和`border`|
|`min-height`|设置**内容区**宽|^|一般不与`height`一起使用|
|`max-height`|设置**内容区**宽|^|^|

+ `content`在不设置`box-sizing`的情况下会受以上表格的属性以及`overflow`属性(滚动条挤占`content`空间)的影响

---

#### ③内边距padding

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`padding-top`|调整上内边距|CSS长度单位，详情见[长度笔记](#css_length)|无|
|`padding-right`|调整上内边距|^|无|
|`padding-bottom`|调整上内边距|^|无|
|`padding-left`|调整上内边距|^|无|
|`padding`|复合属性|^|可以设置1~4个值:<br>1.设置一个值时:四个方向内边距全都为该值<br>2.设置两个值时:上下内边距取决于第一个值，左右内边距取决于第二个值<br>3.设置三个值时，上内边距取决于第一个值，左右内边距取决于第二个值，下内边距取决于第三个值<br>4.设置四个值时，四个值按照上、右、下、左的顺序依次生效|

+ `padding`的值不能是负数
+ **行内元素**的左右内边框的调整是没有问题的，但其上下内边框调整可能会**导致与上侧或下侧元素重叠等问题**，即**上下内边距不能完美设置**
+ **块级元素**和**行内块元素**的四个内边距都可以完美的设置

---

#### ④边框border

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`border-width`|同时定义盒子四个方向的边框宽度|CSS长度单位，详情见[长度笔记](#css_length)|浏览器默认值一般为`3px`,由于屏幕缩放的影响，在开发者工具上显示的大小不一定为`3px`|
|`border-color`|同时定义盒子四个方向的边框颜色|CSS颜色表示，详情见[颜色部分笔记](#css_color)|浏览器默认值一般为黑色|
|`border-style`|同时定义盒子四个方向的边框种类|1.`none`:默认值<br>2.`solid`:实线<br>3.`dashed`:虚线<br>4.`dotted`:点线<br>5.`double`:双实线<br>...|1.`input`标签的默认样式将该属性设置为`inset`，该值在元素被设置`border-radius`时可能会**导致元素出现左上边框颜色较深的情况**|
|`border`|以上属性的复合属性|以上属性值|无顺序要求，用空格隔开|
|`border-left-width`<br>`border-left-color`<br>`border-left-style`|定义盒子左侧边框的宽度/颜色/种类|同上|无|
|`border-left`|以上属性的复合属性|同上|无|
|`border-right-width`<br>`border-right-color`<br>`border-right-style`|定义盒子右侧边框的宽度/颜色/种类|同上|无|
|`border-right`|以上属性的复合属性|同上|无|
|`border-top-width`<br>`border-top-color`<br>`border-top-style`|定义盒子顶部边框的宽度/颜色/种类|同上|无|
|`border-top`|以上属性的复合属性|同上|无|
|`border-bottom-width`<br>`border-bottom-color`<br>`border-bottom-style`|定义盒子底部边框的宽度/颜色/种类|同上|无|
|`border-bottom`|以上属性的复合属性|同上|无|

+ 注意:

> 1. **`border`会随页面缩放比例的大小变化而变化**，如果设置了边框而让页面缩放，可能会使原本规整的页面变的奇形怪状
> 2. 在考虑上述情况的同时，想要移除border对页面的变化，一种解决方法方法是将border属性删除，并使用其他方式来替代此样式，但删除时还应**注意`margin`塌陷问题**

---

#### ⑤外边距margin

##### Ⅰ属性总览

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`margin-left`|定义左外边距|CSS长度单位，详情见[长度笔记](#css_length)|无|
|`margin-right`|定义右外边距|^|无|
|`margin-top`|定义上外边距|^|无|
|`margin-bottom`|定义下外边距|^|无|
|`margin`|复合属性，可以写`1`~`4`个值，规律同`padding`复合属性|^|无|

---

##### Ⅱ注意事项

> 1. 子元素的`margin`是根据父元素的`content`来计算的，因为**父元素的`content`包裹着子元素**
> 2. 调整`margin-top`和`margin-left`都会使元素**自己的位置**发生变化，但调整`margin-bottom`和`margin-right`则会影响后面**兄弟元素的位置**
> 3. **块级元素和行内块元素都可以**完美的设置自己的四个方向的`margin`，但**行内元素只可以**设置`margin-left`和`margin-right`,`margin-top`和`margin-bottom`即使设置了也对其无效
> 4. `margin`值可以为`auto`，如果给一个**块级元素**设置了左右`margin`为0，则它会相对于父元素水平居中
> 5. `margin`值可以为负

---

##### Ⅲmargin塌陷问题

+ margin塌陷问题:第一个子元素的`margin-top`和最后一个子元素的`margin-boottom`会作用在父元素上
[网页示例](../笔记代码/源码/HTML+CSS/CSS_margin塌陷.html)
+ 解决方法:
> 1. 给父元素**设置`padding`宽度不为0**的样式
> 2. 给父元素**设置`border`宽度不为0**的样式
> 3. 给父元素**设置`css`样式`overflow: hidden;`**
> 4. 给父元素设置适当高度(可以解决`margin-bottom`导致的塌陷)(可能不行)

---

##### Ⅳmargin合并问题

+ 上面兄弟元素的**下外边距**和下面兄弟元素的**上外边距**会合并，取一个最大的值，而不是相加。
+ 该问题仅适用于位置在上下的兄弟元素，不适用于位置在左右的兄弟元素

---

## 八、内容处理

### （一）处理内容溢出

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`overflow`|溢出内容的处理方式|1.`visible`:默认值，溢出内容依然显示<br>2.`hidden`:隐藏溢出内容<br>3.`scroll`:显示滚动条，无论内容是否溢出<br>4.`auto`:自动显示滚动条，若不溢出则不显示|1.除能处理溢出外，还能解决一些其他问题(如`margin`塌陷问题)<br>2.如果使用了`overflow`属性且出现了滚动条，**该滚动条默认会挤占盒子的`content`空间**|[溢出处理样例](../笔记代码/源码/HTML+CSS/CSS_内容溢出处理.html)|
|`overflow-x`|水平方向溢出内容的处理方式|同上|**`CSS`的实验性功能，不推荐使用**|^|
|`overflow-y`|垂直方向溢出内容的处理方式|^|**`CSS`的实验性功能，不推荐使用**|^|

---

### （二）元素隐藏

|方法|描述|备注|
|:---:|:---:|:---:|
|`display: none;`|使用元素显示模式样式调整|元素被隐藏，且**不再占据原来的位置**|
|`visibility: hidden`|使用专门的隐藏样式调整(默认值为`show`)|元素被隐藏，但**依然占据原来的位置**|

---

### （三）元素默认样式

+ 元素一般都有一些默认样式:

|元素|默认样式|
|:---:|:---:|
|`<a>`|下划线、字体颜色、鼠标小手|
|`<h1>~<h6>`|加粗、文字大小、上下外边距|
|`<p>`|上下外边距|
|`<ul>`、`<ol>`|左内边距|
|`<body>`|`8px`外边距|

+ **元素的默认样式优先级是大于继承样式的**

---

### （四）布局技巧

[块元素水平垂直居中](../笔记代码/源码/HTML+CSS/CSS_块元素水平垂直居中.html)
[行内元素水平垂直居中](../笔记代码/源码/HTML+CSS/CSS_行内元素水平垂直居中.html)
[行内元素+行内块元素水平垂直居中](../笔记代码/源码/HTML+CSS/CSS_行内元素+行内块元素水平垂直居中.html)

---

### （五）空白问题

#### ①元素间的空白问题

+ 原因:由于写代码时**换行**导致浏览器识别时自动会加一个空格，导致相邻元素之间出现空白
+ 解决方案:
> 1. 由于是换行导致的，把代码写在一行即可(**不推荐**)
> 2. 将这些元素的父元素的`font-size`值设置为0px，但若元素内有文本，需要再写`font-size`属性

---

#### ②行内块元素的幽灵空白问题

+ 原因:行内块元素在作为子元素时，由于其`vertical-align`值默认为`baseline`(基线对齐),而基线并非最低端，因此导致基线下面出现了空白区域
+ 解决方案:
> 1. 手动给该行内块元素设置`vertical-align`值
> 2. 给行内块元素设置`display: block;`但若其后面有文本，该方案不适用
> 3. 给行内块元素的父元素设置`font-size: 0px`，但若内部有文本，还需另外设置`font-size属性`

---

## 九、浮动

+ **浮动**最早期设计出来是为了实现[文字环绕](../笔记代码/源码/HTML+CSS/CSS_浮动_文字环绕.html)效果的，但现在浮动是最主流的页面布局方式之

|属性|含义|属性值|备注|
|:---:|:---:|:---:|:---:|
|`float`|设置浮动|1.`left`:左浮动<br>2.`right`:右浮动<br>3.`none`:默认值，不浮动|无|
|`clear`|指定一个元素是否必须移动 (清除浮动后) 到在它之前的浮动元素下面|1.`none`:默认值,不指定<br>2.`left`:移动到之前的左浮动元素下面<br>3.`right`:移动到之前的右浮动元素下面<br>4.`both`:既需要在左浮动元素下面，也需要在右浮动元素下面|无|

### （一）元素获得浮动后的特点

> 1. 浮动会使元素**脱离文档流**(文档流:各元素按照其特性有顺序的布局)，可以理解为浮动会使元素飘起来，并拜托其元素特性，按照浮动的规则进行布局
> 2. 设置元素浮动后，后面的元素会**填充**到浮动元素原来的空间上，但其内容会在浮动元素内容后显示
> 3. 不管浮动前是什么元素，在浮动后:其**默认宽高都是被内容撑开，但可以通过CSS手动设置**
> 4. 浮动的元素**不会独占一行**，可以与其他元素共用一行
> 5. 不会出现`margin`合并问题，也不会出现`margin`塌陷问题，能够完美的设置四个方向的`margin`和`padding`。
> 6. `text-align`和`line-height`不会对浮动元素生效，即**不会像行内块一样被当做文本处理**(也就不会出现行内块的幽灵空白问题)

---

### （二）一些浮动样例

[左浮动样例](../笔记代码/源码/HTML+CSS/CSS_浮动_左浮动样例.html)
[全浮动样例1](../笔记代码/源码/HTML+CSS/CSS_浮动_全浮动样例1.html)
[全浮动样例2_父元素宽度限制](../笔记代码/源码/HTML+CSS/CSS_浮动_全浮动样例2.html)
[全浮动样例3_盒子高度限制](../笔记代码/源码/HTML+CSS/CSS_浮动_全浮动样例3.html)

---

### （三）浮动的影响

> 1. 后面的兄弟元素会**占据**浮动元素之前的位置(对行内块元素和行内元素不起作用)，对前面的兄弟元素无影响
> 2. 若父元素未设置高度，**则不能撑起父元素的高度**，导致父元素高度塌陷，但会受到父元素的宽度限制
> [浮动影响样例](../笔记代码/源码/HTML+CSS/CSS_浮动_浮动的影响.html)

---

### （四）解决浮动影响

方案:
> 1. 直接给父元素**设置适当高度**:这样可以解决影响2，但**不能解决影响1**
> 2. 给父元素也**设置浮动**:可以解决影响2，但**不能解决影响1**，且影响1还扩散到了父元素
> 3. 给父元素**设置`overflow: hidden;`**:可以解决影响2，但会**加大影响1的力度**，溢出的内容会被隐藏
> 4. 在所有浮动元素后面**加上一个块元素，并设置`clear: both;`**:在其**前面的元素全部都是浮动状态时可以完美解决影响1和影响2**。但只要其前面元素有一个元素不是浮动状态，则问题依然无法解决。
> 5. 使用伪元素选择器作用父元素的最后一个子元素`父元素::after{ content: ''; display: block; clear: both;}`:该方法本质上与方法4相同，但**看起来更牛逼**。[解决浮动影响例](../笔记代码/源码/HTML+CSS/CSS_浮动_解决浮动影响.html)
> + [clear属性详解](https://developer.mozilla.org/zh-CN/docs/Web/CSS/clear)

推荐:
+ 设置浮动时，兄弟元素要么跟着一起全浮动，要么整体全都不浮动

---

### （五）浮动布局

+ [浮动布局](../笔记代码/源码/HTML+CSS/CSS_浮动_浮动布局.html)

---

<a id="position"></a>

## 十、定位

|属性|描述|值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`position`|设置元素的定位方式|1.`relative`:设置相对定位<br>2.`absolute`:设置绝对定位<br>3.`fixed`:设置固定定位<br>|无|[相对定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_相对定位.html)<br>[绝对定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_绝对定位.html)<br>[固定定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_固定定位.html)<br>[粘性定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_粘性定位.html)|

### （一）相对定位

+ 使用`position: relative;`即可开启元素的相对定位
+ 可以使用`left`、`right`、`top`、`bottom`四个属性详细设置元素的相对位置(**可以为负值**)，其相对的是**自己原来应该所在的位置**，如果值为百分比，则是**父元素**大小的百分比
+ 特点:
> 1. 相对定位**不会使元素脱离文档流**，它仅表现为视觉上的变化，对其他元素无影响
> 2. 定位元素的层级显示比普通元素高(定位元素若与普通元素重叠则会**覆盖**普通元素)，若发生重叠的元素都有定位，则根据元素的(不是CSS)代码顺序，遵照**后来者居上**的原则判断谁在最上层
> 3. 不同定位的层级优先级是**相同**的
> 4. **`left`与`right`不能一起使用**，若使用了`left`会生效，而`right`不会。**`top`与`bottom`也不能一起使用**，若一起使用则`top`生效而`bottom`不生效
> 5. 相对定位的元素，**支持通过`margin`属性调整位置**，也**支持浮动**，但不推荐这样做
> 6. **大多数情况下，相对定位会配合绝对定位一起使用**
> 7. 绝对定位**不受父元素宽高的限制**，可以随意调整位置
+ [相对定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_相对定位.html)

---

### （二）绝对定位

+ 使用`position: absolute;`可以开启绝对定位
+ 绝对定位也能使用`top`、`left`、`right`、`bottom`进行位置调整(**可以为负值**)
特点:
> 1. 一旦元素使用了绝对定位，它将**脱离文档流**，并对父元素以及后面的兄弟元素产生影响(对兄弟元素的影响不会像浮动一样会使内容环绕，兄弟元素的内容依旧会在该元素内正常的区域)
> 2. 绝对定位是参考**包含块**的位置来进行位置调整的
>    + 对于**未脱离文档流**的元素，包含块就是**父元素**
>    + 对于**脱离了文档流**的元素，包含块是**第一个拥有定位的祖先元素**(如果全都没有则默认参考页面左上方，即HTML标签)
>    + 一般都会给父元素或其它祖先元素**设置成相对定位**来让绝对定位参考此包含块来进行位置调整
> 3. 若元素设置了宽或高，则:**`left`与`right`不能一起使用**，若使用了`left`会生效，而`right`不会。**`top`与`bottom`也不能一起使用**，若一起使用则`top`生效而`bottom`不生效
> 4. 绝对定位、浮动**不能同时设置**，如果同时设置，则浮动会失效
> 5. 绝对定位的元素，也**能通过`margin`调整位置**，一般来说，`margin`可以调整的方向取决于绝对定位调整的方向。如绝对定位写了`top`和`left`，则只能调整对应的`margin-top`或`margin-left`而不能调整另外两个
> 6. 元素被设置为绝对定位后，其类型会变为**定位元素**，主要特点为:1.默认宽高都被内容撑开2.可以通过CSS直接调整宽高
> 7. 绝对定位**不受父元素宽高的限制**，可以随意调整位置
+ [绝对定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_绝对定位.html)

---

### （三）固定定位

+ 使用`position: fixed;`来开启固定定位
+ 固定定位也能使用`top`、`left`、`right`、`bottom`进行位置调整(**可以为负值**)
+ 特点:
> 1. 一旦元素使用了固定定位，它将**脱离文档流**
> 2. **`left`与`right`不能一起使用，`top`与`bottom`也不能一起使用**
> 3. 固定定位与浮动**不能同时设置**，如果同时设置，则浮动会失效
> 4. 固定定位的元素，也**能通过`margin`调整位置**，一般来说，`margin`可以调整的方向取决于绝对定位调整的方向。如绝对定位写了`top`和`left`，则只能调整对应的`margin-top`或`margin-left`而不能调整另外两个
> 5. 固定定位参考**网页视口**(一个网页当前能看到的区域)来进行位置调整，因此不管如何调整网页的滚动条，它会一直显示在视口的固定位置
> 6. 无论是什么元素设置为固定定位之后，都变成了定位元素
+ [固定定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_固定定位.html)

---

### （四）粘性定位

+ 使用`position: sticky;`来开启固定定位
+ 粘性定位使用`top`、`left`、`right`、`bottom`来选择元素在什么位置被黏住(**可以为负值**)
+ 特点:
> 1. 粘性定位**不会脱离文档流**，该定位是一种专门用于窗口滚动时的新的定位方式
> 2. **`left`与`right`不能一起使用，`top`与`bottom`也不能一起使用**
> 3. 粘性定位的元素，**支持通过`margin`属性调整位置**，也**支持浮动**，但不推荐这样做
> 4. 粘性定位与相对定位的特性基本是**一致**的
+ [粘性定位样例](../笔记代码/源码/HTML+CSS/CSS_定位_粘性定位.html)

---

### （五）定位层级

> 1. **定位元素的层级显示比普通元素高**，但无论什么定位，层级都是一样的
> 2. 如果位置发生重叠，默认情况是：根据元素(不是CSS)的代码顺序，后**面的元素会显示在前面元素之上**
> 3. 可以通过CSS属性值`z-index`手动设置定位元素的优先级，但要注意**只有定位元素才可以设置`z-index`值**
> 4. `z-index`的值为纯数字，没有单位，**值越大层级越大**
> 5. 如果`z-index`值大的元素没有覆盖`z-index`值小的元素，可能是**其包含块的`z-index`值比该元素小**
> [定位层级](../笔记代码/源码/HTML+CSS/CSS_定位_定位层级.html)

---

### （六）总结

|定位类型|>|>|>|位置调整|>|>|>|特点|备注|
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
|**类型**|`top`:调整到上方的距离|`left`:调整到左方的距离|`right`:调整到右方的距离|`bottom`:调整到下方的距离|**是否脱离文档流**|**支持`margin`**|**支持浮动**|**支持过渡**|**参考对象**|无|
|相对定位|^|^|^|^|否|是|是|**否**|**自己原来应该所在的位置**|无|
|绝对定位|^|^|^|^|是|是|否|^|**包含块(离自己最近的拥有定位的祖先元素)(默认为html)**|无|
|固定定位|^|^|^|^|是|是|否|^|**视口**|无|
|粘性定位|^|^|^|^|否|是|是|^|**离自己最近的拥有滚动机制的祖先元素(默认为body)**|无|

---

### （七）定位的特殊应用

+ 注意事项:
> 1. 发生固定定位、绝对定位后，元素都变成了**定位元素**，宽高都被内容撑开，且可以通过`CSS`设置宽高
> 2. 发生相对定位后，元素依然是之前的显示模式(该独占一行还是独占一行...)
> 3. 以下的特殊应用，仅适用于**绝对定位**和**固定定位**的元素，不包括**相对定位**
> 4. 定位是可以越过padding的

#### ①让定位元素的宽高充满包含块

+ 前提:**定位元素未设置宽或高**
+ 若想填满包含块的宽，设置`left: 0px; right: 0px`即可
+ 若想填满包含块的高，设置`top: 0px; bottom: 0px`即可
+ 使用`width: 100%;`或`height: 100%;`是不正确的，因为**属性`width`和`height`仅能设置内容区的宽高**，如果将值设为了100%，那么**如果盒子设置了`padding`或`border`，那么它将从包含块中溢出**
+ [样例](../笔记代码/源码/HTML+CSS/CSS_定位_填满包含块.html)

---

#### ②让定位元素在包含块中居中

+ 前提条件:**定位块必须有明确的宽高**
+ [样例](../笔记代码/源码/HTML+CSS/CSS_定位_包含块中居中.html)

---

## 十一、布局

### （一）版心

+ 在`PC`端网页中，一般都有一个宽度固定且水平居中的盒子，来显示网页的主体内容，称之为**版心**
+ 目前，版心的宽度一般在`960px~1200px`之间
+ 版心可以为1个，也可以为多个
+ 一些网页也会使用[响应式布局](https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Responsive_Design)，使用了响应式布局的网页，版心的作用可能会被弱化

---

### （二）常用类名

|位置|类名|
|:---:|:---:|
|顶部导航条|`topbar`|
|页头|`header`、`page-header`|
|导航|`nav`、`navigator`、`navbar`|
|搜索框|`search`、`search-box`|
|横幅、广告、宣传图|`banner`|
|主要内容|`content`、`main`|
|侧边栏|`aside`、`sidebar`|
|页脚|`footer`、`page-footer`|

---

### （三）重置默认样式

+ HTML早期，由于默认样式的出现，可以加快网页的绘制，但现在因为网页设计越来越复杂，内容越来越多且很精细，默认样式就会对网页的绘制产生不必要的麻烦，因此需要重置默认样式

  1. 使用全局选择器
     + 该方法选择的是所有元素，但不是所有元素都有默认样式，而且有些样式需要做特定处理，因此**基本不推荐使用**，写小项目的时候或者测试时可以写一下
  2. `reset.css`
     + 一般来说，不同公司和团体的`reset.css`代码内容是不同的，因为它**没有一个统一的标准**，但它的目的都是为了清空元素的默认样式
  3. `Normalize.css`
     + `Normalize.css`是一种最新方案，它在清除默认样式的基础上，保留了一些有价值的默认样式，且其**标准是统一的**
     + [官网](http://necolas.github.io/normalize.css/)
     + 相比`reset.css`，`Normalize.css`有以下优点:
       + 保护了有价值的默认样式，而不是完全去掉它们
       + 为大部分HTML元素提供一般化的样式
       + 新增对`HTML5`元素的设置
       + 对并集选择器的使用比较谨慎，有效避免调试工具杂乱
       + `Normalize.css`对默认样式处理更加温和

---

## 十二、画网页

+ [尚品汇](../笔记代码/文件/小项目/尚品汇/代码/index.html)
+ 该内容来源于[尚硅谷](https://www.bilibili.com/video/BV1p84y1P7Z5/?p=149&spm_id_from=pageDriver&vd_source=9264f15df2048663a23ad60d7c291c72)

---

## 十三、CSS3

### （一）CSS3简介

+ `CSS3`是`CSS2`的升级版本，它在`CSS2`的基础上，新增了很多强大的新功能，从而解决一些实际面临的问题
+ `CSS3`在未来会按照模块化的方式去发展：[官方发展规划](https://www.w3.org/Style/CSS/current-work.html)
+ `CSS3`的新特性如下：
>   1. 新增了**更加实用的选择器**，例如：动态伪类选择器、目标伪类选择器、伪元素选择器等等
>   2. 新增了**更好的视觉效果**，例如：圆角、阴影、渐变等
>   3. 新增了**丰富的背景效果**，例如：支持多个背景图片，同时新增了若干个背景相关的属性
>   4. 新增了**全新的布局方案** —— 弹性盒子
>   5. 新增了**一些`Web`字体**，可以显示用户电脑上没有安装的字体
>   6. 增强了[颜色](#css_color)，例如： HSL 、 HSLA 、 RGBA 几种新的颜色模式，新增`opacity`属性来控制透明度
>   7. 增加了`2D`和`3D`变换，例如：旋转、扭曲、缩放、位移等
>   8. 增加**动画与过渡效果**，让效果的变换更具流线性、平滑性
>   ...

---

### （二）私有前缀

+ `W3C`标准所提出的某个`CSS`特性，在被浏览器正式支持之前，浏览器厂商会根据**浏览器的内核**，使用私有前缀来**测试**该`CSS`特性，在浏览器正式支持该`CSS`特性后，就不需要私有前缀了
+ [查询CSS3兼容性的网站](https://caniuse.com/)
+ 常见的浏览器私有前缀有:

> + `Chorme`、`Safari`、`Edge`浏览器:`-webkit-`
> + `Firefox`浏览器:`-moz-`
> + 旧`Opera`浏览器:`-o-`
> + 旧`IE`浏览器:`-ms-`

+ 使用样例:

~~~css
    div{
        /*带有私有前缀的css3样式应该写在前面*/
        -webkit-border-radius: 20px;
        -moz-border-radius: 20px;
        -ms-border-radius: 20px;
        -o-border-radius: 20px;
        border-radius: 20px;
        /*最后用没有写私有前缀的css3样式兜底*/
    }
~~~

+ 注意:一般的主流浏览器都支持常用的`css3`新特性

---

### （三）新增长度单位

|单位|含义|备注|样例|
|:---:|:---:|:---:|:---:|
|`rem`|**根元素字体**大小的倍数|仅与根元素字体大小有关，若根元素未设置，则取决于默认浏览器字体大小|[样例](../笔记代码/源码/HTML+CSS/CSS_长度单位.html)|
|`vh`|**视口高度**的百分之多少|随视口高度的变化而变化|[样例](../笔记代码/源码/HTML+CSS/CSS3_新增长度单位.html)|
|`vw`|**视口宽度**的百分之多少|随视口宽度的变化而变化|^|
|`vmax`|**视口宽高中大的**那个的百分之多少|随视口的变化而变化|^|
|`vmin`|**视口宽高中小的**那个的百分之多少|^|^|

+ 这些长度单位的意义在于使得一些元素可以灵活的根据视口的变化而调整自身宽高

---

### （四）新增盒子属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`box-sizing`|设置盒模型的两种类型|1.`content-box`:标准盒模型，`height`与`width`仅能设置内容区大小<br>2.`border-box`:怪异盒模型,`height`和`width`设置的是盒子整体的大小(即`content+padding+border`)|无|[box-sizing样例](../笔记代码/源码/HTML+CSS/CSS3_新增盒子属性_box-sizing.html)|
|`resize`|设置是否允许用户调整盒子大小|1.`none`:默认值，不允许用户调整<br>2.`both`:允许用户调整盒子的宽与高<br>3.`vertical`:仅允许用户调整盒子的高<br>4.`horizontal`:仅允许用户调整盒子的宽|**该属性必须配合`overflow`属性一起使用**|[resize样例](../笔记代码/源码/HTML+CSS/CSS3_新增盒子属性_resize.html)|
|`box-shadow`|为盒子添加阴影|见下表|1.`box-shadow: none;`为默认值，表示没有阴影<br>2.阴影不会与相邻兄弟元素争抢空间，而是会被**覆盖**|[box-shadow样例](../笔记代码/源码/HTML+CSS/CSS3_新增盒子属性_box-shadow.html)|
|`opacity`|为元素设置不透明度|从`0~1`之间的小数,0为完全透明，1为完全不透明|无|[opacity样例](../笔记代码/源码/HTML+CSS/CSS3_新增盒子属性_opacity.html)|

+ `box-shadow`的格式为:`box-shadow: h-shadow v-shadow blur spread color inset;`

|属性值|含义|备注|
|:---:|:---:|:---:|
|`h-shadow`|阴影的水平位置|必须填写,可以为负值|
|`v-shadow`|阴影的垂直位置|必须填写,可以为负值|
|`blur`|模糊距离|可选值|
|`spread`|外延距离|可选值|
|`color`|阴影颜色|可选值，默认为黑色|
|`inset`|将外部阴影改为内部阴影|可选值|

---

### （五）新增背景属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`background-origin`|设置背景图的原点|1.`padding-box`:默认值，从`padding`的左上角的点开始显示背景图<br>2.`border-box`:从`border`的左上角的点开始显示背景图<br>3.`content-box`:从`content`的左上角的点开始显示背景图|无|[background-origin样例](../笔记代码/源码/HTML+CSS/CSS3_新增背景属性_background-origin.html)|
|`background-clip`|设置背景图的截止区域|1.`border-box`:默认值，`border`以外的区域不再显示背景图<br>2.`padding-box`:`padding`以外的区域不再显示背景图<br>3.`content-box`:`content`以外的区域不再显示背景图<br>4.`text`:背景图仅呈现在文字上(**实验性属性值**)|1.该属性可以与`background-origin`属性一起使用<br>2.若想使用`text`属性值，需要给属性加上**私有前缀**，并**设置文字颜色为`transparent`(透明)**<br>3.该属性也会对`background-color`有所作用|[background-clip样例](../笔记代码/源码/HTML+CSS/CSS3_新增背景属性_background-clip.html)|
|`background-size`|设置背景图的大小|1.`auto`:默认值，将背景图的左上方点与原点对齐后根据盒子的范围显示背景图<br>2.像素值:手动设置背景图的高与宽，**不允许负值**<br>3.百分比:用盒子宽高的百分比指定背景图大小,不允许负值<br>4.`contain`:将背景图片等比缩放，使背景图片的宽或高，与容器的宽或高相等，再将完整背景图片包含在容器内，但要注意：**可能会造成容器里部分区域没有背景图片或图片重复(使用`backgound-repeat`可去除)**<br>5.`cover`:将背景图片等比缩放，直到完全覆盖容器，多余的部分将被隐藏，图片会尽可能全的显示在元素上，但要注意：**背景图片有可能显示不完整**。—— **相对比较好的选择**|无|[background-size样例](../笔记代码/源码/HTML+CSS/CSS3_新增背景属性_background-size.html)|
|`background`|复合属性|格式为:`background: color url repeat position / size origin clip`|1.`origin`与`clip`的值如果一样，如果只写一个值，那么同时设置这两个属性，如果写了两个值，那么按照`origin`,`clip`的先后顺序来设置<br>2.`size`的值必须写在`position`的后面，并用符号`/`分开<br>3.有一些属性不按顺序写也是可以的,但还是推荐按照格式写|[background样例](../笔记代码/源码/HTML+CSS/CSS3_新增背景属性_background复合属性.html)|
|多背景图|该情况由`background`复合属性实现|占位|不能使用`background-image`属性来设置多背景图，因为它不支持|[多背景图样例](../笔记代码/源码/HTML+CSS/CSS3_新增背景属性_多背景图样例.html)|

---

### （六）新增边框属性

+ `CSS3 `新增了盒子的**外轮廓**，外轮廓无法被开发者工具显示且不占位

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`border-radius`|设置边框圆角|像素值:设置边框四个角处四分之一圆弧的半径|无|[border-radius样例](../笔记代码/源码/HTML+CSS/CSS3_新增边框属性_border-radius.html)|
|^|^|百分比:根据盒子宽高设置边框四个角处四分之一圆弧的半径|无|^|
|^|^|复合属性|1.格式为:`border-radius: 左上椭圆水平半径 右上椭圆水平半径 左下椭圆水平半径 右下椭圆水平半径 / 左上椭圆垂直半径 左上椭圆垂直半径 左上椭圆垂直半径 左上椭圆垂直半径`，必须按照此格式写<br>2.`input`标签的默认样式将该属性设置为`inset`，该值在元素被设置`border-radius`时可能会**导致元素出现左上边框颜色较深的情况**|^|
|`border-top-left-radius`|设置左上角圆角半径|1 一个值是正圆半径<br>2.两个值分别是椭圆的x半径、y半径。|无|^|
|`border-top-right-radius`|设置右上角圆角半径|^|^|^|
|`border-bottom-left-radius`|设置左下角圆角半径|^|^|^|
|`border-bottom-right-radius`|设置右下角圆角半径|^|^|^|
|`outline-width`|外轮廓宽度|[CSS长度单位](#css_length)|无|[outline-样例](../笔记代码/源码/HTML+CSS/CSS3_新增边框属性_outline.html)|
|`outline-color`|外轮廓颜色|[CSS颜色](#css_color)|无|^|
|`outline-style`|外轮廓风格|`none`:无轮廓、`dashed`:虚线轮廓、`dotted`:点状轮廓<br>`solid`:实线轮廓、`double`:双线轮廓|无|^|
|`outline-offset`|设置外轮廓与边框的距离|[CSS长度单位](#css_length)|1.可以设置负值<br>2.该属性不是`outline`的子属性，而是独立的属性|^|
|`outline`|外轮廓复合属性|可以设置宽度、颜色、风格|1.无顺序要求<br>2.该复合属性不包括`outline-offset`，因为该属性为独立属性|^|

---

### （七）新增文本属性

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`text-shadow`|为文本添加阴影|语法:`text-shadow: h-shadow(必填项，调整水平位置) v-shadow(必填项，调整垂直位置) blur(选填，模糊距离) color(选填，阴影颜色)`|无|[text-shadow样例](../笔记代码/源码/HTML+CSS/CSS3_新增文本属性_text-shadow.html)|
|`white-space`|设置文本换行方式|1.`normal`:默认值，文本超出边界自动换行(溢出时不会超盒子的宽度溢出),文本中的换行会被浏览器识别为一个空格<br>2.`pre`:与`pre`标签的效果一致(**如果单行文本长度大于盒子宽度会超出盒子宽度溢出**)<br>3.`pre-wrap`:在属性值`pre`的基础上，超出元素边界自动换行(即不会超出盒子宽度溢出),但**依旧识别空格和换行**<br>4.`pre-line`:在`pre`属性值的基础上，超出元素边界自动换行，且**仅识别文本间的空格，文本首尾的空格会被忽略**<br>5.`nowrap`:强制不换行(会超出盒子宽度溢出)，但识别空格|无|[white-space样例](../笔记代码/源码/HTML+CSS/CSS3_新增文本属性_white-space.html)|
|`text-overflow`|处理文本溢出|1.`clip`:默认值。当内联内容溢出时，将溢出部分裁切掉<br>2.`ellipsis`:当内联内容溢出块容器时，将溢出部分替换为`...`|要使`text-overflow`属性生效，块容器必须显式定义`overflow`属性为非`visible`值，且`white-space`的属性值为`nowrap`值|[text-overflow样例](../笔记代码/源码/HTML+CSS/CSS3_新增文本属性_text-overflow.html)|
|`text-decoration`|CSS3使该属性变成了复合属性，可以设置线条位置，线条形状和线条颜色|可以按照`line`(调整线条位置)、`style`(调整线条形状)、`color`(线条颜色)|1.[详情查看上文](#text-decoration)<br>2.这三个属性的子属性分别为`text-decoration-line`、`text-decoration-style`、`text-decoration-color`|[文本修饰样例](../笔记代码/源码/HTML+CSS/CSS_文本_文本修饰.html)|
|`-webkit-text-stroke`|设置文本描边(复合属性)|格式:`-webkit-text-stroke: width color`|1.该属性仅有`webkit`内核的浏览器支持，且需要使用私有前缀<br>2.必须将宽度与颜色全部设置才能生效|[text-stroke样例](../笔记代码/源码/HTML+CSS/CSS3_新增文本属性_text-stroke.html)|
|`-webkit-text-stroke-width`|设置文本描边宽度|[CSS长度单位](#css_length)|^|^|
|`-webkit-text-stroke-color`|设置文本描边颜色|[CSS颜色](#css_color)|^|^|

---

### （八）渐变

+ **渐变的本质是背景图**，因此它需要使用`background-image`属性来调用

#### ①线性渐变

+ 线性渐变可以在`background-image`属性内写下`linear-gradient()`并在括号内写入**渐变方向**、**渐变位置**和**渐变颜色**及**渐变起始位置**
+ 渐变方向可以使用**关键词(top、bottom、right、left)调整**，也可以使用**角度调整**
+ 渐变位置在颜色后面写一个空格，**支持百分比和像素值**
+ 格式为:`background-image: linear-gradient( to position,color1 length,color2,color3, .....);`
+ [线性渐变样例](../笔记代码/源码/HTML+CSS/CSS3_渐变_线性渐变.html)

---

#### ②径向渐变
+ 径向渐变可以在`background-image`属性内写下`radial-gradient()`并在括号内写入**半径**、**圆心位置**和**渐变颜色**及**渐变起始位置**
+ **径向渐变的图形形状由其所在元素的宽高比决定**，因此大多数情况下一般为椭圆形
+ 半径可以通过像素值进行设置，同时也可以通过**关键字`circle`直接设置为圆**或通过**关键字`ellipse`设置为椭圆**，猜测：如果使用关键字设置，那么其半径大小应该取决于其所在元素的大小
+ 圆心位置可以通过写**一对像素值**确定，第一个值为水平距离，第二个值为垂直距离
+ 渐变起始位置与渐变颜色保持一个空格，**支持百分比和像素值**
+ [径向渐变样例](../笔记代码/源码/HTML+CSS/CSS3_渐变_径向渐变.html)

---

#### ③重复渐变

+ 重复渐变的前提是线性渐变或径向渐变
+ 重复渐变重复的前提是**线性渐变或径向渐变**的一些部分没有发生渐变，这种情况一般发生在给渐变设置了起始位置的情况
+ [重复渐变样例](../笔记代码/源码/HTML+CSS/CSS3_渐变_重复渐变.html)
+ [渐变样例-横格纸与立体球](../笔记代码/源码/HTML+CSS/CSS3_渐变_渐变案例.html)

---

### （九）字体

#### ①定制字体

+ CSS3支持了浏览器引用不存在于电脑上的字体，它可以通过**识别文件路径**或**网上地址**的方式直接使用一些定制字体了
+ 简写方式:
~~~css
    @font-face {
        font-family: "情书字体";
        src: url('../方正手迹.ttf');
    }
    div{
        font-family: '情书字体';
    }
~~~
+ 语法(高兼容性写法):
~~~css
    @font-face {
        font-family: "atguigu";
        font-display: swap;
        src: url('webfont.eot'); /* IE9 */
        src: url('webfont.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
        url('webfont.woff2') format('woff2'),
        url('webfont.woff') format('woff'), /* chrome、firefox */
        url('webfont.ttf') format('truetype'), /* chrome、firefox、opera、Safari,
        Android*/
        url('webfont.svg#webfont') format('svg'); /* iOS 4.1- */
    }
    div{
        font-family: 'atguigu';
    }
~~~

---

#### ②字体图标

+ 可以在[阿里巴巴矢量图标库](https://www.iconfont.cn/?spm=a313x.collections_index.i3.d4d0a486a.56473a81Zececr)选择图标下载
+ 具体参考[图标使用样例](../笔记代码/文件/杂项/字体图标/font_4240210_pxm2y60hv9/demo_index.html)

---

### （十）变换

#### ①2D变换

+ `2D`变换使用属性`transform`来实现
+ **所有的`2D`变换属性都不能作用于行内元素**

|值/属性|描述|备注|样例|
|:---:|:---:|:---:|:---:|
|`translateX`|设置水平方向位移，需指定长度值|若指定的是百分比，是**参考自身宽度**的百分比|[位移样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D位移.html)|
|`translateY`|设置垂直方向位移，需指定长度值|若指定的是百分比，是**参考自身高度**的百分比|^|
|`translate`|设置位移，需指定长度值|1.一个值代表水平方向，两个值代表：水平和垂直方向<br>2.两个值时，**值之间用逗号隔开**|^|
|`scaleX()`|设置**水平方向**的缩放比例|1.值为一个数字(**不需要写单位**),`1`表示不缩放,大于`1`放大,小于`1`缩小<br>2.可以写负值，负值的效果是盒子的左半边与盒子的右半边互换|[缩放样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D缩放.html)|
|`scaleY()`|设置**垂直方向**的缩放比例|1.值为一个数字(**不需要写单位**),`1`表示不缩放,大于`1`放大,小于`1`缩小<br>2.可以写负值，负值的效果是盒子的上半边与盒子的下半边互换|^|
|`scale()`|同时设置水平方向、垂直方向的缩放比例|1.一个值代表**同时设置水平和垂直缩放**<br>2.两个值分别代表：水平缩放、垂直缩放(用逗号隔开)<br>3.可以写负值|^|
|`rotate`|设置旋转角度，需指定一个角度值(单位:`deg`)|正值顺时针，负值逆时针|[旋转样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D旋转.html)|
|`skewX`|设置元素在水平方向扭曲，值为角度值(单位:`deg`)|可以写负值|[扭曲样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D扭曲.html)|
|`skewY`|设置元素在垂直方向扭曲，值为角度值(单位:`deg`)|^|^|
|`skew`|设置元素扭曲|一个值代表`skewX`，两个值分别代表:`skewX`、`skewY`|^|
|多重变换|`transform`属性可以同时设置多个值|缩放不会影响坐标系位置，旋转会影响坐标系位置|[多重变换样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_多重变换.html)|
|`transform-origin`|设置变换原点|变换原点的改变**不会影响位移变换**，但**会影响旋转**(会围绕原点旋转)**和缩放**(向原点靠拢或远离)|[变换原点样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_变换原点.html)|

---

##### Ⅰ2D位移

+ 编写`transform`的具体值，可选值如下:

|值|描述|备注|
|:---:|:---:|:---:|
|`translateX`|设置水平方向位移，需指定长度值|若指定的是百分比，是**参考自身宽度**的百分比|
|`translateY`|设置垂直方向位移，需指定长度值|若指定的是百分比，是**参考自身高度**的百分比|
|`translate`|设置位移，需指定长度值|1.一个值代表水平方向，两个值代表：水平和垂直方向<br>2.两个值时，**值之间用逗号隔开**|

+ 注意:
> 1. 位移和相对定位很相似，它们都不脱离文档流，也**不会影响到其它元素**
> 2. 位移与相对定位的区别是:相对定位的百分比值，参考的是其**父元素**；而位移的百分比值，参考的是其**自身**
> 3. 浏览器针对位移有优化，与定位相比，**浏览器处理位移的效率更高**
> 4. `transform` 可以链式编写，例如：`transform: translateX(30px) translateY(40px);`
> 6. 位移配合定位，可实现元素水平垂直居中。
>     + 例：
>     + ~~~css
>       .box {
>           position: absolute;
>           left: 50%;
>           top: 50%;
>           transform: translate(-50%, -50%);
>       }
>       ~~~
+ [位移样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D位移.html)

---

##### Ⅱ2D缩放

+ 编写`transform`的具体值，可选值如下:

|值|描述|备注|
|:---:|:---:|:---:|
|`scaleX()`|设置**水平方向**的缩放比例|1.值为一个数字(**不需要写单位**),`1`表示不缩放,大于`1`放大,小于`1`缩小<br>2.可以写负值，负值的效果是盒子的左半边与盒子的右半边互换|
|`scaleY()`|设置**垂直方向**的缩放比例|1.值为一个数字(**不需要写单位**),`1`表示不缩放,大于`1`放大,小于`1`缩小<br>2.可以写负值，负值的效果是盒子的上半边与盒子的下半边互换|
|`scale()`|同时设置水平方向、垂直方向的缩放比例|1.一个值代表**同时设置水平和垂直缩放**<br>2.两个值分别代表：水平缩放、垂直缩放(用逗号隔开)<br>3.可以写负值|

+ 注意:
> 1. 借助缩放，可实现小于`12px`(浏览器默认最小字体大小)的文字

+ [缩放样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D缩放.html)

---

##### Ⅲ2D旋转

+ `2D`旋转是指：让元素在二维平面内，顺时针旋转或逆时针旋转
+ 元素可以绕`X`轴、`Y`轴或`Z`轴旋转，在这三个坐标轴中，不难理解，**只有绕`Z`轴旋转是2D的**，因此此处仅说明元素绕`Z`轴旋转的情况
+ 编写`transform`的具体值，可选值如下:

|值|描述|备注|
|:---:|:---:|:---:|
|`rotate`|设置旋转角度，需指定一个角度值(单位:`deg`)|正值顺时针，负值逆时针|
+ 注意:
> 1. `rotateZ(20deg)`相当于`rotate(20deg)`，因为当`rotate`仅写一个值的时候默认处理为2D旋转

+ [旋转样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D旋转.html)

---

##### Ⅳ2D扭曲

+ `2D`扭曲是指：让元素在二维平面内被“拉扯”，进而“走形”，
+ 编写`transform`的具体值，可选值如下:

|值|描述|备注|
|:---:|:---:|:---:|
|`skewX`|设置元素在水平方向扭曲，值为角度值(单位:`deg`)|可以写负值|
|`skewY`|设置元素在垂直方向扭曲，值为角度值(单位:`deg`)|^|
|`skew`|设置元素扭曲|一个值代表`skewX`，两个值分别代表:`skewX`、`skewY`|

+ [扭曲样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_2D扭曲.html)

---

##### Ⅴ多重变换

+ 如果缩放与位移共同作用时，根据二者代码位置的不同，效果也会不同，因为**缩放不会影响坐标轴的位置**
+ 如果旋转与位移共同作用时，根据二者代码位置的不同，效果也会不同，因为**旋转会让坐标轴也跟着旋转**
+ [样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_多重变换.html)
+ 建议:**使用变换时将旋转放在最后面**

---

##### Ⅵ变换原点

+ 变换原点:默认为元素的**正中心**
+ 使用`transform-origin`可以设置变换的原点,可以写**像素值、关键词、百分比**等，参考的是元素本身大小
+ 如果提供两个值，第一个作用于横坐标，第二个作用于纵坐标
+ 如果提供一个值，若为**像素值**，那么作用在横坐标上，**纵向默认为50%**；若为关键词，根据关键词的作用域，另一个坐标默认为50%
+ 变换原点的改变**不会影响位移变换**，但**会影响旋转**(会围绕原点旋转)**和缩放**(向原点靠拢或远离)
+ [样例](../笔记代码/源码/HTML+CSS/CSS3_2D变换_变换原点.html)

---

#### ②3D变换

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`transform-style`|设置元素的子元素是位于3D空间中还是平面中|1.`flat`:设置元素的子元素位于该元素的平面中<br>2.`preserve-3d`:指示元素的子元素应位于 3D 空间中|该属性是为其作用元素的子元素设置3D变换的前提|[3D空间与景深样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D空间与景深.html)|
|`perspective`|指定了观察者与z=0平面的距离，使具有三维位置变换的元素产生透视(近大远小)效果|1.`none`:默认值，无景深效果<br>2.[css长度单位](#css_length)|值不能为负值|^|
|`perspective-origin`|指定观察者的位置,用作`perspective`属性的消失点|关键词、像素值和百分比|1.百分比值参考盒子本身大小<br>2.设置单值，则另一方默认为`center`（应该）|^|
|`translate3d`|同时调整x、y、z轴的位移|1.格式`transform: translated(x,y,z)`<br>2.z轴的值只能写像素值，**不能写百分比，因为没有参考对象**|[3D位移样例](./笔记代码/源码/HTML+CSS/CSS3_3D变换_3D位移.html)|
|`translateZ`|调整z轴位移|只能写像素值，**不能写百分比，因为没有参考对象**|^|
|`rotateX`|设置元素绕X轴旋转(需要设置角度值)|无|[3D旋转样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D旋转.html)|
|`rotateY`|设置元素绕Y轴旋转(需要设置角度值)|无|^|
|`rotate3d`|设置元素旋转|里面有四个值，第四个值表示单位旋转角度，**参数不允许省略**|^|
|`scaleZ`|设置`z`轴方向的缩放比例，值为一个数字|无|[3D缩放样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D缩放.html)|
|`scale3d`|设置三个方向的缩放比例|按照x、y、z的顺序进行设置，**参数不允许省略**|^|
|`backface-visibility`|设定是否隐藏背部镜像|1.`visible`:指定元素背部可见，默认值<br>2.`hidden`:指定元素背面不可见|[背部样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_多重变换与背部.html)|

##### Ⅰ3D空间与景深

+ 想要设置3D变换效果，需要首先给**目标元素的父元素**设置3D空间(`transform-style`)与景深(`perspective`)

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`transform-style`|设置元素的子元素是位于3D空间中还是平面中|1.`flat`:设置元素的子元素位于该元素的平面中<br>2.`preserve-3d`:指示元素的子元素应位于 3D 空间中|该属性是为其作用元素的子元素设置3D变换的前提|[样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D空间与景深.html)|
|`perspective`|指定了观察者与z=0平面的距离，使具有三维位置变换的元素产生透视(近大远小)效果|1.`none`:默认值，无景深效果<br>2.[css长度单位](#css_length)|值不能为负值|^|
|`perspective-origin`|指定观察者的位置,用作`perspective`属性的消失点|关键词、像素值和百分比|1.百分比值参考盒子本身大小<br>2.设置单值，则另一方默认为`center`（应该）|^|

---

##### Ⅱ3D位移

|值|描述|备注|
|:---:|:---:|:---:|
|`translate3d`|同时调整x、y、z轴的位移|1.格式`transform: translated(x,y,z)`<br>2.z轴的值只能写像素值，**不能写百分比，因为没有参考对象**|
|`translateZ`|调整z轴位移|只能写像素值，**不能写百分比，因为没有参考对象**,且**参数不能省略**|

+ [样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D位移.html)

---

##### Ⅲ3D旋转

+ `3D`旋转是在`2D`旋转的基础上，可以让元素沿`x`轴或`y`轴旋转

|值|描述|备注|
|:---:|:---:|:---:|
|`rotateX`|设置元素绕X轴旋转(需要设置角度值)|无|
|`rotateY`|设置元素绕Y轴旋转(需要设置角度值)|无|
|`rotate3d`|设置元素旋转|里面有四个值，第四个值表示单位旋转角度，**参数不允许省略**|

+ [样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D旋转.html)

---

##### Ⅳ3D缩放

+ `3D`缩放是在`2D`缩放的基础上，可以让元素沿`z`轴缩放

|值|描述|备注|
|:---:|:---:|:---:|
|`scaleZ`|设置`z`轴方向的缩放比例，值为一个数字|无|
|`scale3d`|设置三个方向的缩放比例|按照x、y、z的顺序进行设置，**参数不允许省略**|

+ 对`z`轴方向设置缩放，但是元素并没有厚度，因此**设置缩放实际上为等比例缩放景深**
  + 如一个盒子，其父元素景深为`500px`，设置`scaleZ`为`4`，实际上与父元素景深为`500/4px`，`scaleZ`为`1`效果是相同的

+ [样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_3D缩放.html)

---

##### Ⅴ多重变换与背部

|属性|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`backface-visibility`|设定是否隐藏背部镜像|1.`visible`:指定元素背部可见，默认值<br>2.`hidden`:指定元素背面不可见|无|[样例](../笔记代码/源码/HTML+CSS/CSS3_3D变换_多重变换与背部.html)|

+ 一个`transform`属性内部可以写多个值，但要注意:
  1. 旋转会改变坐标轴位置
  2. 缩放不会改变坐标轴位置
  3. 原点位置可以通过`transform-origion`属性改变

---

### （十一）过渡

+ 过渡可以在不使用`JavaScript`的情况下，让元素从一种样式，平滑过渡为另一种样式
+ 注意事项:
> 1. **所有[定位](#position)都不支持过渡**

|值/属性|描述|备注|样例|
|:---:|:---:|:---:|:---:|
|`transition-property`|设置什么属性需要过渡|**不是所有属性都能够过渡**的，只有**值为数字**(如`height`、`width`等)，或**值可以转换为数字**(如`background-color`)的属性才能过渡|[transition-property样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_指定过渡元素与时间.html)|
|`transition-duration`|设置过渡的持续时间|若想让每个属性过渡时都有不同的持续时间，需要手动将每个属性对应的时间(单位:`s`或`ms`)都写上去，**用逗号隔开**|^|
|`transition-delay`|设置开始过渡的延迟时间|无|^|
|`transition-timing-function`|设置过渡类型|该属性的值过多，详情见下文或样例|[timing-function样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_过渡类型.html)|
|`transition`|复合属性|该属性不限制先后顺序，但写的第一个时间值作为`duration`属性值，第二个值作为`delay`属性值，若仅写一个时间值，则`delay`属性默认为0，该时间值作用于`duration`属性|[复合属性样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_复合属性.html)|

#### ①设置过渡属性

+ `transition-property`属性可以设置什么属性需要过渡，**只有在该属性中被定义的属性才会有过渡效果**
+ 常用值有:
  1. `none`:默认值，不过渡任何属性
  2. `all`:过渡所有能够过渡的属性
  3. `具体属性名称`:如`height`、`width`等，如果有多个，需要**用逗号隔开**
+ 注意:
> 1. **不是所有属性都能够过渡**的，只有**值为数字**(如`height`、`width`等)，或**值可以转换为数字**(如`background-color`)的属性才能过渡
> 2. 常见的支持过渡的属性有:颜色属性、长度值属性、百分比值属性、`z-index`、定位属性、变换属性、阴影属性、`opacity`等

+ [transition-property样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_指定过渡元素与时间.html)

---

#### ②设置过度持续时间

+ `transition-duration`属性可以设置过渡的持续时间
  + 常用值有:
    1. `0`:无任何过渡时间(单位:`s`或`ms`)
    2. 单一值:将所有被选中的过渡的属性过渡时间都设置为此值(单位:`s`或`ms`)
    3. 若想让每个属性过渡时都有不同的持续时间，需要手动将每个属性对应的时间都写上去，**用逗号隔开**
+ `transition-delay`属性可以设置开始过渡的延迟时间

+ [过渡时间样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_指定过渡元素与时间.html)

---

<a id="transition-timing-function"></a>

#### ③设置过渡类型

+ `transition-timing-function`属性可以设置过渡类型
+ 常用值:
  1. `ease`:平滑过渡(先慢后快再慢，初始速度较快),默认值
  2. `linear`:匀速过渡
  3. `ease-in`:速度先慢后快
  4. `ease-out`:速度先快后慢
  5. `ease-in-out`:速度先慢后快(初始速度较慢)
  6. `step-start`:忽略过渡时间，直接实现完全过渡
  7. `step-end`:不忽略过渡时间，等待过渡时间完成后直接实现完全过渡
  8. `steps( integer,?)`:接受两个参数的步进函数，第一个参数决定过渡时间内要走的步数，第二个参数取值可以为`start`和`end`，默认值为`end`,指定每一步发生变化的时间点(取`start`值在开始过渡的一瞬间就跨出第一步，取`end`值要等(过渡时间/步数)的时间后再跨出第一步)
  9. `cubic-bezie ( number, number, number, number)`:特定的[贝塞尔曲线](https://cubic-bezier.com/)类型(网站中的横坐标为时间，纵坐标为距离)

+ [timing-function样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_过渡类型.html)

---

#### ④复合属性与过渡小案例

> + `transition`是过渡的复合属性，可以同时写`duration`、`delay`、`timing-function`、`property`属性
> + 该属性不限制先后顺序，但写的第一个时间值作为`duration`属性值，第二个值作为`delay`属性值，若仅写一个时间值，则`delay`属性默认为0，该时间值作用于`duration`属性
+ [复合属性样例](../笔记代码/源码/HTML+CSS/CSS3_过渡_复合属性.html)
+ [过渡小案例](../笔记代码/源码/HTML+CSS/CSS3_过渡小案例.html)

---

### （十二）动画

#### ①动画定义

+ 使用以下方式来**简单定义**动画，`from`表示开始关键帧状态设置,`to`表示结束时关键帧状态设置
~~~css
    @keyframes 动画名 {
        from {
            /*property1:value1*/
            /*property2:value2*/
        }
        to {
            /*property1:value1*/
        }
    }
~~~

+ 使用以下方式来**完整定义**动画,百分比可以设置动画演示到这一进度时的元素状态

~~~css
    @keyframes 动画名 {
        0% {
            /*property1:value1*/
            /*property2:value2*/
        }
        100% {
            /*property1:value1*/
        }
    }
~~~

---

#### ②属性与案例

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`animation-name`|引用动画|定义的动画名称|无|[动画样例](../笔记代码/源码/HTML+CSS/CSS3_动画_基本使用.html)|
|`animation-duration`|动画持续时间|时间(单位`m`或`ms`)|无|^|
|`animation-delay`|设置动画延迟|^|无|^|
|`animation-timing-function`|设置动画的类型|详情参考[过渡类型](#transition-timing-function)的属性值|无|^|
|`animation-iteration-count`|指定动画的播放次数|1.数值<br>2.`infinite`:无限循环|无|^|
|`animation-direction`|指定动画方向|1.`normal`:默认正常方向(从左到右)<br>2.`reverse`:反方向运行<br>3.`alternate`: 动画先正常运行再反方向运行，并持续交替运行<br>4.`alternate-reverse`:动画先反运行再正方向运行，并持续交替运行|后两个属性值要实现，必须保证`animation-iteration-count`的值`>=2`|^|
|`animation-fill-mode`|设置动画在执行之前和之后如何将样式应用于其目标|1.`forwards`:设置对象状态为动画结束时的状态<br>2.`backwards`:设置对象状态为动画开始时的状态|无|^|
|`animation-play-state`|设置动画的播放状态|1.`running`:运动<br>2.`paused`:暂停|该属性一般单独使用|^|
|`animation`|复合属性|以上全部属性值|无顺序，第一个时间被用来表示动画演示时间，第二个时间被用来表示动画延迟演示时间|^|
|>|>|>|>|[动画案例](../笔记代码/源码/HTML+CSS/CSS3_动画案例.html)|

---

#### ③动画与过渡的区别

1. **过渡必须要有触发条件**，而动画不受此限制
2. 过渡只能决定开始和结束的元素状态，而**动画可以精细的调整元素在整个动画期间的状态**

---

### （十三）多列布局

+ 多列布局适用于制作类似于报纸的布局

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`column-count`|指定分隔的列数|数字|若属性出现冲突，浏览器会选择占用空间(即列数)小的呈现|[多列布局-文本样例](../笔记代码/源码/HTML+CSS/CSS3_多列布局-文本样例.html)<br>[多列布局-图片样例](../笔记代码/源码/HTML+CSS/CSS3_多列布局_图片样例.html)|
|`column-width`|指定分隔的列数|[css长度单位](#css_length)|^|^|
|`columns`|复合属性,同时指定列宽和列数|数字|如果设置的值冲突，浏览器会选择占用空间(即列数)小的呈现|^|
|`column-gap`|设置列边距|[css长度单位](#css_length)|1.默认间距为一个字符的间距<br>2.即使将值设置成`0`，也会有最小的间距|^|
|`column-rule-style`|设置列与列之间边框的风格|1.`none`:默认值<br>2.`solid`:实线<br>3.`dashed`:虚线<br>4.`dotted`:点线<br>5.`double`:双实线<br>...|无|^|
|`column-rule-width`|设置列与列之间边框的宽度|[css长度单位](#css_length)|无|^|
|`column-rule-color`|设置列与列之间边框的颜色|数字|无|^|
|`column-rule`|设置列边框，复合属性|数字|无顺序要求|^|
|`column--span`|指定是否跨列|1.`none`:不跨列<br>2.`all`:跨列|**该属性作用于对应元素，而不是对应元素的父元素**|^|

---

### （十四）伸缩盒模型

#### ①简介

+ `2009`年，`W3C`提出了一种新的盒子模型——`Flexible Box`(**伸缩盒模型**，又称**弹性盒子**)
+ 他可以轻松地控制:元素分布方式、元素对齐方式、元素视觉顺序...
+ 截止到目前，除了在部分在`IE`浏览器不支持，其它浏览器均已全部支持
+ 伸缩盒模型的出现，逐渐演变出了一套新的布局方案 —— `flex`布局
> 1. 传统布局是指：基于传统盒状模型，主要靠：`display`属性+`position`属性+`float`属性
> 2. `flex`布局**目前在移动端应用比较广泛**，因为传统布局不能很好的呈现在移动设备上

---

#### ②伸缩容器与伸缩项目

+ **伸缩容器**: 开启了`flex`的元素，就是：伸缩容器
    > 1. 给元素设置` display:flex`或`display:inline-flex`，该元素就变成了伸缩容器
    > 2. `display:inline-flex`很少使用，因为可以给多个伸缩容器的父容器，也设置为伸缩容器
    > 3. 一个元素可以同时是：伸缩容器、伸缩项目
+ **伸缩项目**:伸缩容器所有**子元素**自动成为了：伸缩项目
    > 1. 仅伸缩容器的子元素成为了伸缩项目，**孙子元素、重孙子元素等后代，不是伸缩项目**
    > 2. 伸缩项目默认会水平对齐
    > 3. 无论原来是哪种元素（块、行内块、行内），一旦成为了伸缩项目，全都会“**块状化**”，即拥有块元素的特性。
+ [样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_容器与项目.html)

---

#### ③主轴与侧轴

+ 主轴:**伸缩项目沿着主轴排列**，主轴默认是**水平**的，默认方向是：从左到右（左边是起点，右边是终点）
+ 侧轴:**与主轴垂直**的就是侧轴，侧轴默认是**垂直**的，默认方向是：从上到下（上边是起点，下边是终点）
> + **侧轴方向会随主轴方向的变化而变化**

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`flex-direction`|设置主轴方向|1.`row`:默认值，沿行向延伸，方向一般从左到右<br>2.`row-reverse`:沿行向延伸,方向一般从右到左<br>3.`column`:主轴方向沿块向(纵向)从页面上侧延伸到下侧<br>4.`column-reverse`:主轴方向沿块向(纵向)从页面下侧延伸到上侧|**侧轴方向会随主轴方向的变化而变化**|[方向与换行样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_主轴属性样例1.html)|
|` flex-wrap`|设置主轴换行方式|1.`nowrap`:默认值，不换行<br>2.`wrap`:正向换行<br>3.`wrap-reverse`:反向换行<br>|^|^|
|`flex-flow`|上面两个属性的复合属性|上面的属性值|无顺序要求，一般不用|^|
|` justify-content`|设置主轴对齐方式|1.`flex-start`:默认值，主轴起对齐<br>2.`flex-end`:主轴终点对齐<br>3.`center`:居中对齐<br>4.`space-between`:项目均匀分布，项目与项目之间距离**相同**，项目与边界无距离<br>5.`space-around`:项目均匀分布，项目与项目的距离是项目与边界距离的**二倍**<br>6.`space-evenly`:项目均匀分布|无|[主轴对齐样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_主轴属性样例2.html)|
|`align-items`|设置侧轴单行对齐方式|1.`flex-start`:按侧轴的起点对齐<br>2.`flex-end`:侧轴的终点对齐<br>3.`center`:侧轴的中点对齐<br>4.`baseline`:基线对齐<br>5.`stretch`:**默认值**,如果伸缩项目未设置高度，将占满整个容器的高度|无|[单行对齐样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_侧轴单行对齐.html)|
|`align-content`|设置侧轴多行对齐方式|1.`flex-start`:按侧轴的起点对齐<br>2.`flex-end`:侧轴的终点对齐<br>3.`center`:侧轴的中点对齐<br>4.`space-between`:项目与两端无距离，项目与项目之间距离相等<br>5.`space-around`:项目与项目之间的距离相等，且为项目与两端距离的二倍<br>6.`space-evenly`:项目均匀分布<br>7.`stretch`:**默认值**，占满整个侧轴|无|[单行对齐样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_侧轴多行对齐.html)|


---

#### ④伸缩盒的伸与缩

+ **这些样式全都是在伸缩项目内设置的**

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`flex-grow`|定义伸缩项目的放大比例|数值(无单位)|各盒子分到的大小为:`盒子当前属性值(长或宽)*(该盒子对应flex-grow属性/全部flex-grow属性和)`<br>2.使用该属性的**前提条件为不换行**<br>3.默认值为`0`|[样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_伸与缩样例.html)|
|`flex-shrink`|定义伸缩项目的缩小比例|数值(无单位)|1.各盒子缩小的大小为:`盒子当前属性值(长或宽)*(该盒子理论长或宽*对应flex-grow属性/盒子理论长或宽*对应flex-grow属性的和)`<br>2.如果计算结果有一些误差，大概率为`border`的问题<br>3.使用该属性的**前提条件为不换行**<br>4.默认值为`1`<br>5.**如果父容器过小，该项目会缩小到一定程度(保证其内容的正常显示)后不再缩小**|^||
|`flex-basis`|指定`flex`元素在主轴方向上的初始大小|1.`auto`:伸缩项目的宽和高<br>2.[css长度值](#css_length)|浏览器根据这个属性设置的值，计算主轴上是否有多余空间|^||
|`flex`|以上三个属性的复合属性|见上|顺序:`flex: flex-grow flex-shrink flex-basis`|^|

---

#### ⑤项目排序与单独对齐

|属性/值|含义|属性值|备注|样例|
|:---:|:---:|:---:|:---:|:---:|
|`order`|定义项目的排列顺序，数值越小，排列越靠前|数值|默认值为`0`|[样例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_项目排序与单独对齐.html)|
|`align-self`|单独调整某个伸缩项目的对齐方式|数值|默认值为`auto`,表示继承父元素的`align-items`属性|^|

---

#### ⑥伸缩盒小案例

+ [伸缩盒案例](../笔记代码/源码/HTML+CSS/CSS3_伸缩盒_伸缩盒小案例.html)

---

### （十五）媒体查询

+ 注意:
> 1. 在媒体查询内的css语句与其它css语句相比其**优先级是一样的**
> 2. 必须将媒体查询**写在对应样式后**才能生效，因为写在前面会被后面的效果覆盖

#### ①查询媒体类型

+ 语法:`@media 值{ css代码 }`
+ 媒体查询的值:

|值|含义|
|:---:|:---:|
|`all`|检测所有设备|
|`screen`|检测电子屏幕，包括：电脑屏幕、平板屏幕、手机屏幕等|
|`print`|检测打印机|
|`aural`|**已废弃**,用于语音和声音合成器|
|`braille`|**已废弃**,应用于盲文触摸式反馈设备|
|`embossed`|**已废弃**,用于打印的盲人印刷设备|
|`handheld`|**已废弃**,用于掌上设备或更小的装置，如PDA和小型电话|
|`projection`|**已废弃**,用于投影设备|
|`tty`|**已废弃**, 用于固定的字符网格，如电报、终端设备和对字符有限制的便携设备|
|`tv`|**已废弃**, 用于电视和网络电视|
+ 完整属性参考[mdn文档](https://developer.mozilla.org/zh-CN/docs/Web/CSS/@media)
+ [样例](../笔记代码/源码/HTML+CSS/CSS3_媒体查询_媒体类型样例.html)

---

#### ②媒体特性

|值|含义|
|:---:|:---:|
|`width`|检测**视口宽度**|
|`max-width`|检测**视口最大宽度**|
|`min-width`|检测**视口最小宽度**|
|`height`|检测**视口高度**|
|`max-height`|检测**视口最大宽度**|
|`min-height`|检测**视口最小宽度**|
|`device-width`|检测**设备宽度**,设备的宽度与操作系统的缩放有关|
|`max-device-width`|检测**设备最大宽度**,设备的宽度与操作系统的缩放有关|
|`min-device-width`|检测**设备最小宽度**,设备的宽度与操作系统的缩放有关|
|`orientation`|检测视口的**旋转方向**（是否横屏）<br>1.`portrait`:视口处于纵向，即高度大于等于宽度<br>2.`landscape`:视口处于横向，即宽度大于高度|
+ 完整属性参考[mdn文档](https://developer.mozilla.org/zh-CN/docs/Web/CSS/@media)
+ [媒体特性样例](../笔记代码/源码/HTML+CSS/CSS3_媒体查询_媒体特性样例.html)

---

#### ③运算符

|值|含义|备注|
|:---:|:---:|:---:|
|`and`|与运算符|无|
|`or`或`,`|或运算符|无|
|`not`|非运算符|无|
|`only`|肯定运算符|主要用于处理`IE`浏览器的兼容性问题，因为`IE`浏览器支持媒体类型，但不支持媒体特性|
+ [运算符样例](../笔记代码/源码/HTML+CSS/CSS3_媒体查询_运算符.html)

---

#### ④常用阈值

+ 一般分成`768px`(小屏幕)、`992px`(中屏幕)、`1200px`(大屏幕)、更大的为超大屏幕
+ 结合外部样式，可以这样引入css
    1. ~~~html
        <link rel="stylesheet" media="具体的媒体查询" href="mystylesheet.css">
       ~~~
    2. ~~~css
            @media screen and (max-width:768px) {
            /*CSS-Code;*/
            }
            @media screen and (min-width:768px) and (max-width:1200px) {
            /*CSS-Code;*/
            }
            ...
       ~~~

---

### （十六）BFC

#### ①BFC介绍

> 1. `W3C`:原文：Floats, absolutely positioned elements, block containers (such as inline-blocks, table, cells, and table-captions) that are not block boxes, and block boxes with 'overflow' other than 'visible' (except when that value has been propagated to the viewport) establish new block formatting contexts for their contents.
译文：浮动、绝对定位元素、不是块盒子的块容器（如 inline-blocks 、 table-cells 和table-captions ），以及 overflow 属性的值除 visible 以外的块盒，将为其内容建立新的块格式化上下文。
> 2. `MDN`描述:块格式化上下文（Block Formatting Context，BFC）是 Web 页面的可视 CSS 渲染的一部分，是块盒子的布局过程发生的区域，也是浮动元素与其他元素交互的区域。
> 3. 人话:
>    1. **`BFC`是 `Block Formatting Context`（块级格式上下文）**，可以理解成元素的一个“特异功能”
>    2. 该 “特异功能”，在**默认的情况下处于关闭状态**；当元素满足了某些条件后，该“特异功能”被激活。
>    3. 所谓激活“特异功能”，专业点说就是：该元素创建了`BFC`（又称：开启了`BFC`）

---

#### ②BFC能解决的问题

1. 元素开启`BFC`后，其子元素不会再产生`margin`塌陷问题
2. 元素开启`BFC`后，自己不会被其他浮动元素所覆盖
3. 元素开启`BFC`后，就算其子元素浮动，元素自身高度也不会塌陷

---

#### ③BFC开启条件

1. 根元素
2. 浮动元素
3. 绝对定位、固定定位的元素
4. 行内块元素
5. 表格单元格:`table`、`thead`、`tbody`、`tfoot`、`th`、`td`、`tr`、`caption`
6. `overflow`的值不为`visible`的块元素
7. 伸缩项目
8. 多列容器
9. `column-span`为`all`的元素（即使该元素没有包裹在多列容器中）
10. `display`的值，设置为`flow-root`

---




